# Open findings — and the record of when this file was itself wrong

아홉 차례 리뷰를 돌렸다(2026-08-27~28). **판정이 나온 여덟 번 모두 지적이 있었다**(7차는 공급자 콘텐츠 필터에 걸려 판정 자체가 안 나왔다). 매번 지적을
결함 주입으로 재현하고 고쳤다. 이 파일은 세 가지를 담는다:

1. **아직 안 고친 것** — 한계를 한계로 적는다
2. **「닫았다」고 적었다가 틀린 것** — 지우지 않는다
3. **닫힌 것** — 「고쳤다」가 아니라 「어떻게 증명했나」

고친 내역은 CHANGELOG 에 있다.

---

## 1. 아직 안 고친 것

### C-5 계약은 5섹션인데 검사는 「아무 H2 5개」

개수는 계약과 맞췄다(4 → 5). 그러나 **개수는 정체를 못 본다** — 제목을 `잡담 1~5` 로
바꿔도 clean 이다.

locale 중립을 지키며 닫으려면 **기계 판독 마커**가 필요하고(`## 핵심 역할 <!--harness:role-->`),
그건 생성 형식을 바꾸는 일이라 기존 하네스의 마이그레이션 경로가 같이 필요하다.
지금은 **흔한 실패(절이 빠졌다)는 잡고 적대적 실패(제목만 다섯 개)는 못 잡는다.**

### C-8 워크플로우 파싱은 정규식이다

`.claude/workflows/*.ts` 를 읽고, 주석을 벗기고, 산문용 폴백은 적용하지 않는다.
그래도 정규식이라 남는 것:

| 모양 | 결과 |
|---|---|
| `agent(p, {agentType: roles[i]})` — 변수로 조립 | 호출로 안 보임 → **고아 오진** |
| `agent(p, {meta: {agentType: "x"}})` — 중첩 객체 | 호출로 오인 → 고아를 놓침 |
| 문자열 리터럴 안의 `agentType: "x"` | 호출로 오인 → 고아를 놓침 |

첫 줄만 정상 하네스를 거부하는 방향이다. 닫으려면 TS 토크나이저가 필요하고, 그건 이
검사기의 무게를 크게 바꾼다.

### A-11 기대 팩 집합의 «추가» 는 확인 없이 학습된다

추가는 자동 학습, 삭제는 위반 — 한 방향으로만 움직인다. 그래서 **의도치 않게 들어온
팩도 기준선이 된다.** 승인 단계를 두는 편이 엄격하지만, 마찰이 늘면 검사가 꺼진다는
쪽을 택했다. `learn_expected` 에 확인 단계를 넣으면 바뀐다.

### A-13 같은 슬롯의 동일한 바이트는 구별할 수 없다

스킬 도달 검사(A)는 프롬프트에 실린 레지스트리 줄을 읽는다. 구간을 나누고, 이름과
디렉터리가 같은지 보고, 모르는 줄은 «형식 변경»(exit 2)으로 뺀다. 그래도 **정확히
같은 구간에, 정확히 같은 형식으로, 정확히 같은 바이트를 쓴 위조 줄**은 진짜와 구별되지
않는다. 항목별 provenance(어느 파일에서 왔는지)가 프롬프트에 없기 때문이다.

두 자문(Codex · Gemini)이 독립적으로 같은 결론을 냈다. 닫으려면 런타임이 항목마다
출처를 실어 줘야 하고, 그건 우리가 못 바꾸는 층이다. **그래서 한계로 적는다.**

### C-12 CI 계약은 «이 두 줄 그대로» 다 — 셸을 파싱하지 않는다

「이 셸 조각이 그 스크립트를 실행하는가」를 파싱으로 맞히려던 함수(`_invokes`)는
**네 차례 리뷰에서 연달아 뚫렸다**: `bash -n` · `command -v` · `false && …` ·
`if false; then … fi` · 히어독 · 안 불리는 함수 · `bash -c ':' x.sh` · `-O extglob`.
반대 방향으로도 틀려서 `/bin/bash X` · `sudo -u root bash X` · `-euo pipefail` 같은
정상 표현을 거부했다. 셸 문법은 유한한 목록이 아니고, 우리는 셸을 구현하는 게 아니다.

그래서 **함수를 지우고 계약을 좁혔다.** CI 는 정확히 이 두 줄을 돈다:

```
python3 scripts/validate_repository.py --only preflight-stages
bash scripts/preflight.sh
```

`make` · `just` · `npm run` · `working-directory` 를 바꾼 스텝 · `docker exec` ·
`|| true` 는 전부 **거부**된다. 이건 못 잡는 결함이 아니라 **명시한 계약**이고,
어기면 시끄럽게 거부한다 — 조용히 통과하는 쪽이 아니다.
(8·9차 리뷰어가 같은 방향을 두 번 권고했다.)

### D-9 진입점을 셸이 아니라 파이썬으로 두는 편이 단순하다 (미착수)

8차 리뷰의 구조 권고: `preflight.sh` 는 `exec python3 scripts/preflight_runner.py` 만
하고, 단계를 `STAGES = [(id, label, argv)]` 한 곳에 선언한다. 그러면 단계 수는
`len(STAGES)` 에서 파생되고, **`PREFLIGHT_RUNNER_BODIES`·셸 함수 중복 탐지·실행자 셰도잉
정규식·`STAGES_EXPECTED` 가 대부분 필요 없어진다.**

옳은 방향이라고 본다. 지금 하지 않은 이유는 **현재 판이 35 케이스로 검증돼 있고, 재작성은
그 검증을 통째로 다시 해야 하기 때문**이다. 다음 판의 첫 작업으로 둔다.

### D-8 `exit 0` 은 스크립트 «안에서는» 원리적으로 못 잡는다

`preflight.sh` 의 마지막 줄을 `exit $fail` 에서 `exit 0` 으로 바꾸면 모든 게이트가
빨개져도 스크립트는 성공을 보고한다. **자기 종료코드를 검사하는 코드도 그 종료코드에
덮인다.** 스스로는 닫히지 않는다.

그래서 짝을 밖에 뒀다 — CI 와 pre-push 훅이 preflight «앞에»
`python3 scripts/validate_repository.py --only preflight-stages` 를 돌린다. 이 스텝의
종료코드는 preflight 가 못 건드린다. 남는 한계: 그 스텝 자체를 워크플로우에서 지우는 것
(→ `ci-integrity-step` 케이스가 잡는다)과 D-7 의 «여러 파일을 같이 고치기».

### D-7 preflight 실행기 고정은 두 파일을 같이 고치면 뚫린다

`run()` 을 「특정 단계는 건너뛰게」 고치면 호출 줄도 `STAGES_EXPECTED` 도 그대로인데
그 단계가 안 돈다. 그래서 게이트가 실행기의 **본문을 글자 그대로 고정**한다
(`PREFLIGHT_RUNNER_BODIES`).

🔴 **그 고정만 두었을 때 하루 만에 셋이 더 뚫렸다.** 정적 고정은 「본 적 있는 모양」만
막는다는 것을 내가 직접 재현했다:

| 통과한 변조 | 왜 통과했나 |
|---|---|
| `run()` 을 한 번 더 정의 | 고정이 «첫 줄» 만 봤다. 셸은 나중 정의를 쓴다 |
| `python3() { return 0; }` | 실행자 이름을 셸 함수로 가리면 그 단계가 no-op |
| `exit $fail` → `exit 0` | D-8 |

그래서 판정의 무게중심을 **런타임 강제**로 옮겼다: 가드레일이 판정기를 하나 망가뜨린
사본에서 `bash scripts/preflight.sh` 를 **실제로 실행하고** non-zero 를 요구한다.
경로가 무엇이든 답이 같기 때문이다. 정적 고정은 그 위의 얇은 그물로 남겼다.

남는 한계는 그대로다 — **스크립트는 자기 자신에 대한 편집을 막지 못한다.**
`preflight.sh` 와 `validate_repository.py` 를 **같이** 고치면 통과한다.
리뷰·커밋 단위로 보이게 만드는 것이 여기서 할 수 있는 전부다.

### 거짓양성 후보 (정상인데 거부될 수 있는 것)

- 에이전트 이름을 변수로 조립하는 워크플로우 (C-8)
- 이름에 콜론이 든 스킬(`namespace:skill`)은 도달 판정에서 «미등록» 으로 보인다 —
  공유 팩에는 그런 이름이 없지만, 생기면 시끄러워진다(조용히 통과하지는 않는다)
- 의도적인 project-scope override

---

## 2. 「닫았다」고 적었다가 틀린 것

이 파일이 스스로 부정확했던 기록이다. **지우지 않는다** — 게이트를 만든 사람이 그
게이트를 과신한 사례이고, 그게 이 프로젝트가 반복해 겪은 실패다.

| 무엇 | 무엇이 틀렸나 | 실제로 닫힌 판 |
|---|---|---|
| `ci-runs-preflight` | 파일 전체에서 문자열을 찾아, `run:` 을 바꿔도 **머리말 주석**이 조건을 만족시켰다 | v2.8.2 |
| `orphan-agents` (워크플로우) | 주석 처리된 `// agentType: 'x'` 를 호출로 셌다 | v2.8.2 |
| A-1 (스킬 도달) | 블록을 통째로 훑어 **설명문 안의 예시 줄**이 등록으로 세어졌다 | v2.8.3 |
| C-3 (틸드 펜스) | 구현은 맞았지만 **회귀 테스트가 헛돌았다** — 픽스처 제목 4개 vs 임계값 5 | v2.8.3 |
| `lint-rule-docs` | 자기가 쓰여진 문서 두 개만 보고 clean. **README 3종은 7규칙·4섹션이라 말하고 있었다** | v2.8.3 |
| A-1 (다시) | 줄 앵커만으로는 부족했다 — **항목처럼 생긴 설명 bullet**이 통과했다 | v2.8.4 |
| `ci-runs-preflight` (다시) | `run:` 안의 **셸 주석**만으로 통과했다 | v2.8.4 |
| README 규칙 «개수» | 영어 README 는 여전히 "Seven deterministic rules" 였고 검사가 그 문장을 안 봤다 | v2.8.4 |
| 자기시험 배선 | `verdict()` 보호를 지우면 자기시험만 실패하고 **preflight 는 통과**했다 | v2.8.4 |
| 회귀 케이스 2종 | README 규칙명 검사와 워크플로우 폴백 제외는 **구현을 지워도 가드레일이 통과**했다 | v2.8.4 |
| 배선 그 자체 | v2.8.4 가 자기시험을 preflight 에 넣었는데, **그 한 줄을 지워도 아무도 몰랐다** | v2.9.0 |
| `ci-runs-preflight` (세 번째) | `run: echo scripts/preflight.sh` 가 통과했다 — 「문자열에 있나」로 「실행되나」를 물었다 | v2.9.0 |
| A-1 (세 번째) | 「이름 == 디렉터리」로도 부족했다. 구간을 안 나눠 **설명문의 bullet** 이 항목으로 세어졌다 | v2.9.0 |
| `dead-api` | 「지시문인지」를 부정어 목록으로 알아맞히려 했다. 거짓음성 4 · 거짓양성 1 | v2.9.0 |
| `orphan-agents` | 표의 백틱·`@`·예제 리터럴을 «호출» 로 셌다 | v2.9.0 |
| 패리티 허용쌍 | 방향이 없어 두 문장을 서로 바꿔도 통과했다(v2.8.4 에서 닫음, 이 표에 누락돼 있었다) | v2.8.4 |
| 형식·전멸 계약 | 프롬프트 형식 변경이 exit 1 로, 팩 전멸이 조기 exit 2 로 나갔다(이 표에 누락돼 있었다) | v2.8.4 |
| 배선 (다시) | `run()` 이 단계를 세면서 실행은 건너뛰게 고쳐도 **호출 줄과 개수가 맞아** 전부 초록이었다 | v2.9.1 |
| `_invokes` | **양방향으로 틀렸다** — `bash -n`·`command -v`·`false && …` 를 통과시키고, `/bin/bash …`·`sudo -u root bash …` 를 거부했다 | v2.9.1 |
| 전용 케이스 부재 | `_invokes` 를 부분문자열로 되돌려도, stale allowlist 검사를 통째로 지워도 **가드레일이 초록**이었다 | v2.9.1 |
| `dead-api` 범위 | `build` 를 «경로 조각 어디에나» 적용해 우리가 쓰는 `docs/build/` 가 통째로 스캔 밖이었다 | v2.9.1 |
| 자기시험 (A) | 합성 스텁 시험이 `if codex 있음` 안에 있어, 파서를 깨뜨리면 **아무것도 안 재고 exit 0** 이었다 | v2.9.1 |
| 산문 케이스 (A) | 구간 «안에» 있어서 구간 분리가 아니라 «모르는 줄» 규칙으로 통과했다 — 틀린 이유로 통과 | v2.9.1 |
| 실행기 고정 | v2.9.1 의 고정은 「본 적 있는 모양」만 막았다 — 중복 정의·실행자 셰도잉·`exit 0` 셋이 통과 | v2.9.2 |
| `_invokes` (다시) | 제어흐름을 몰랐다 — `if false; then …; fi`·히어독·안 불리는 함수 몸통을 「실행」으로 셌다 | v2.9.2 |
| `_invokes` (반대 방향) | `bash -eu -o pipefail X` 를 거부했다 — `pipefail` 이 스크립트 자리로 보였다 | v2.9.2 |
| 가드레일 절 | `guardrail_preflight_enforces(…)` 한 줄을 지우면 절이 통째로 사라지는데 아무도 몰랐다 | v2.9.2 |
| 내 작업 방식 | 패치가 「ok」를 찍었는데 **파일에 안 들어가 있었다**. 되읽어 확인하지 않았다 | v2.9.2 |
| 종료 계약 | `exit $fail` 이 «있는지» 만 봤다 — 단계들 위에 `exit 0` 을 놓으면 통과 | v2.9.3 |
| 정의 인식 | `name()` 문법만 봤다 — bash 의 `function run { … }` 로 덮으면 통과 | v2.9.3 |
| 절 호출 검사 | 「불리는가」만 봤다 — 목록을 비우면 돌긴 도는데 아무것도 안 잰다 | v2.9.3 |
| nest 스위치 | `run:` 안에서만 찾았다 — job-level `env:` 로 켜면 통과 | v2.9.3 |
| allowlist 키 | 같은 파일의 **동일한 두 줄**이 키를 공유해, 하나를 승인하면 나머지가 따라 들어왔다 | v2.9.3 |
| 강제 확인 판정 | 「어디서든 non-zero」를 증거로 셌다 — 무관한 실패가 케이스를 영원히 통과시킨다 | v2.9.3 |
| `_invokes` (네 번째) | 계약을 좁혔다면서도 여전히 파싱했다 — `bash -c ':' x.sh` 통과, `-euo pipefail` 거부 | v2.9.4 |
| 정의 인식 (다시) | `\` 줄 연결과 «본문이 다음 줄인 함수» 를 놓쳤다. bash 는 연결을 파싱 전에 지운다 | v2.9.4 |
| 목록 개수 검사 | 선언부만 셌다 — `PREFLIGHT_ENFORCEMENT.clear()` 한 줄이면 정적 검사는 통과 | v2.9.4 |
| 재귀 스위치 | 환경변수라 워크플로우 밖에서 켤 수 있었다(composite action·`$GITHUB_ENV`·스크립트 내 export) | v2.9.4 |
| 종료 계약 (다시) | `exit` 만 봤다 — `fail=0` 재설정과 `trap ... EXIT` 로 같은 결과를 낼 수 있었다 | v2.9.4 |
| 훅 설치 | 「preflight 를 언급하나」로 판단해, preflight 와 «다른 검사도» 돌리는 훅을 백업 없이 지웠다 | v2.9.4 |
| 이 문서 | 자기시험 건수를 낡은 값으로 적어 두고 안 고쳤다 — 문서가 코드보다 뒤처졌다 | v2.9.4 |

**같은 실수가 열여섯 번 나왔다: «있나» 로 «그런가» 를 대신 물었다.**
(스킬 레지스트리 ×3 · CI 게이트 ×4 · 워크플로우 주석 · preflight 실행기 ×3 ·
dead-api 범위·키 ×2 · `_invokes` 제어흐름 · 종료 계약 · 절 호출)

🔑 이 부류는 «한 번 고치면 끝나는 버그» 가 아니라 **기본값**이다. 검사를 쓸 때마다
문장을 소리 내어 읽어라 — 「preflight 가 게이트를 강제한다」를 검사한다면서
`"exit $fail" in 파일` 을 쓰고 있으면 문장과 코드가 다른 말을 하고 있다.

마지막 둘은 부분문자열이 아니라 **축을 잘못 고른 판**이다 —
「호출 줄이 있나」로 「실행되나」를 물었고, 「`build` 라는 이름이 있나」로
「남이 만든 것인가」를 물었다. 답은 같다: **묻고 싶은 것을 그대로 물어라.**

그리고 **반대 방향** 도 두 번 나왔다 — 규칙을 좁히면서 `python3 X` 를 「실행 아님」으로 읽어
멀쩡한 preflight 를 거부했고(거짓양성 4건), 같은 함수가 `/bin/bash X`·`sudo -u root bash X`
도 거부하고 있었다. 🔑 **규칙을 좁힐 때는 정상 입력도 같이 재라.** 이제
그리고 결국 **그 함수 자체를 지웠다** — 아래 C-12 를 보라.

> 🔑 **판정문을 소리 내어 읽어라.** 「CI 가 preflight 를 부른다」를 검사한다면서
> `"preflight" in 파일_전체` 를 쓰고 있으면, 문장과 코드가 다른 말을 하고 있는 것이다.

두 번째 반복 패턴: **틀린 이유로 통과하는 테스트.** 초록인데 겨냥한 것을 안 재고 있었다.
**일곱 번** 나왔다(D-6 · 틸드 펜스 · 회귀 케이스 2종 · orphan 케이스 3종 ·
자기시험 스텁이 닫는 태그를 빠뜨려 「형식 변경」 케이스들이 전부 틀린 이유로 통과 ·
A 의 합성 스텁이 `codex` 없으면 아예 안 돌았음 · 산문 케이스가 구간 분리가 아닌
「모르는 줄」 규칙으로 통과). 픽스처가 소스 소유의 값을 복붙하면
규칙이 바뀔 때 조용히 무의미해진다 — 이제 임계값을 소스에서 읽는다.

---

## 3. 닫힌 것 — 무엇으로 증명했나

| | 무엇이었나 | 무엇으로 닫혔나 |
|---|---|---|
| D-1 | 가드레일이 규칙 이름을 찍고 `exit 2` 로 죽는 가짜 검사기를 합격 | `verdict()` — exit 1 + 규칙 이름 + Traceback 없음. 가짜 주입 시 전 케이스 거부 |
| A-1 | 산문·예시 줄·설명 bullet 이 등록으로 세어짐 | 루트 별칭 표 + **이름 == 디렉터리** + realpath, 가짜 codex 자기시험 |
| A-6 | 「추가→통과→삭제」가 exit 0 | 단방향 학습 + 생애주기 자기시험 |
| A-9 | 다른 루트의 동명이인 통과 | 절대경로 대조 |
| A-12 | SKILL.md 가 «파일인지» 만 확인 | frontmatter `name` 요구 |
| A(계약) | 형식 변경이 exit 1(미도달)로 나감 | exit 2(검사 불가)로 분리 |
| A(계약) | 팩이 전부 사라지면 조기 exit 2 로 조용히 종료 | 기대 목록이 있으면 «전부 사라짐» 위반 |
| B-3/B-4 | 유사도 98%가 의미 반전을, 96%가 절 삭제를 통과 | 섹션 단위 비교. 실제 tracer 드리프트도 닫음 |
| B | 섹션만 봐서 `name`·`description` 변경이 안 보임 | 메타데이터·순서·중복·미닫힘까지 비교 |
| B | 런타임 허용쌍이 대칭이라 두 문장을 **뒤바꿔도** 통과 | 방향성 있는 (섹션, 문장쌍) |
| C-3 | `~~~` 펜스 안의 `##` 를 섹션으로 계산 | CommonMark 펜스 + 임계값을 읽는 회귀 케이스 |
| C-8 | 워크플로우를 안 읽음 / 주석·템플릿리터럴을 호출로 셈 | 워크플로우 독해 + 주석 제거 + 폴백 제외 |
| C-11 | 관찰 불가능한 것을 CI 로 차단 | `uniformTierRationale` 면제 + 유효변형 |
| `dead-api` | 워크플로우를 안 봄 — 제거된 `TeamCreate` 호출이 clean 이었다 | agents + skills + **workflows** |
| FP-1 | 파일명이 다른 전역 에이전트 호출을 오진 | 양쪽 다 frontmatter `name` 으로 해석 |
| FP-2 | 여러 줄 `Agent(...)` 안의, 제거된 `team_name` 인자를 놓침 | 괄호 균형 호출 블록 |
| D-3 | 워킹트리 복사 후 `git add -A` | 실제 추적 경로만 스테이징 |
| D-4/D-5/D-6 | 표기 결합 · 개수 부정확 · 규칙을 안 타는 변형 | 정규식 교체 · 규칙/케이스 분리 표기 · 에이전트 3개 |
| 배선 | preflight 가 강제 경로에 없음 | CI 가 preflight 를 부르고 `ci-runs-preflight` 가 `run:` 단계를 파싱해 검사 |
| 배선 | **자기시험이 게이트에 연결 안 됨** | preflight 첫 단계가 판정기 자기시험. 주간 감사도 A·B 자기시험 먼저 |
| 배선 | `run()` 변조로 단계를 세면서 안 돌림 | **가드레일이 preflight 를 실제로 돌려** non-zero 를 요구. 정적 고정은 보조. 한계는 D-7·D-8 |
| 배선 | `exit 0` 이 모든 실패를 삼킴 | CI·pre-push 가 preflight «앞에» 무결성 스텝. `ci-integrity-step` 케이스 |
| 배선 | 가드레일 절 하나를 지우면 조용히 사라짐 | `REQUIRED_GUARDRAIL_SECTIONS` — 밖에서 호출을 센다 |
| `_invokes` | 제어흐름·히어독·안 불리는 함수를 실행으로 셈 | 무조건 실행되는 줄만, 단 **불리는 함수 몸통은 포함** |
| 재귀 스위치 | 환경변수라 파일 밖에서 켤 수 있었다 | 사본 안의 «파일» 로. 추적되면 게이트가 거부 |
| 강제 확인 목록 | 정적 개수만 셌다 | 실행 시점에도 다시 센다 — 값이 진실이다 |
| 종료 계약 | `exit` 만 봤다 | `exit $fail` 이 마지막 실행 줄 · 리터럴 `exit <n>` 금지 · `fail=0` 1회 · `trap` 금지 |
| 함수 정의 | 문법을 열거해 막으려 했다 | **허용 목록**(`run`·`stage`)으로 뒤집고, 줄 연결을 먼저 결합 |
| 훅 설치 | 문자열로 「우리 것인가」 판단 | 쓸 내용과 **바이트 대조** 후 다르면 백업 |
| 강제 확인 | 아무 데서나 빨개져도 증거로 셈 | 그 «단계 구간» 안에서 실패했는지까지 확인 |
| 소유 판정 | 추적되는 파일도 이름으로 제외 | 추적 = 우리 것. 이름 기반 제외는 추적 안 되는 것에만 |
| 훅 설치 | 남의 `pre-push` 를 백업 없이 덮어씀 | 우리 것이 아니면 타임스탬프 백업 |
| `_invokes` | 실행과 언급을 양방향으로 혼동, 그리고 계속 뚫림 | **함수를 지우고 계약을 좁혔다** — CI 는 정해진 두 줄 그대로(C-12) |
| 가드레일 | 새 규칙에 전용 케이스가 없어 «지워도 초록» | 4 케이스 추가, **각각 구현을 되돌려 `did NOT fail` 을 확인** |
| `dead-api` | 이름으로 소유를 판정 | `vendor`/`dist`/`build` 는 **최상위일 때만** 제외 |
| 문서 | 구현 9규칙 vs 문서 7규칙 | `lint-rule-docs` — SKILL.md·command·**README 3종** |
| 운영 | cron PATH 에 `codex` 없음 | 스크립트가 자기 도구를 찾는다 |
| 운영 | cron 결과를 아무도 안 읽음 | 실패 시 세션 시작에 표시 |
