<!-- Modified from revfactory/harness (Apache-2.0, Copyright 2025 robin).
     Releases 2.1.0 and earlier are upstream's; 2.2.0 is this derivative's. -->

# Changelog

이 프로젝트는 [Semantic Versioning](https://semver.org/)을 따릅니다.

`oh-my-harness` 는 [revfactory/harness](https://github.com/revfactory/harness) 의 파생본입니다.
**2.1.0 이하 항목은 업스트림의 기록**이며, 2.2.0 부터가 이 저장소의 변경입니다.
채택·기각한 업스트림 PR 의 전수 판정은 [docs/ATTRIBUTION.md](docs/ATTRIBUTION.md) 에 있습니다.

## [2.11.0] - 2026-08-28

14차 리뷰. **재지 않은 것은 지켜지지 않았다.**

### 안 재는 다섯 단계는 «실행을 생략해도» 초록이었다

프로브가 여덟 단계 중 셋만 재고 있었다. 나머지 다섯은 러너에서 이 한 줄이면 통째로
사라졌고, preflight 는 rc=0 이었다:

```python
if label == "repository gates":
    continue
```

이제 **여덟 단계 전부에 결함을 심고 여덟이 다 빨간지** 본다. 안 도는 단계는 빨개질 수도
없으므로, 재는 범위가 곧 전부여야 한다. 기대값은 여기 적지 않고 러너의 `STAGES` 에서
읽는다 — 단계를 더하고 결함을 안 심으면 그 단계가 초록으로 돌아와 프로브가 실패한다.
**새 단계는 심을 결함과 함께 들어오라는 뜻이다.**

이 과정에서 계측기 자신이 틀린 것도 하나 나왔다. 요약이 실패 단계를 쉼표로 이어 붙였는데
라벨 하나(`nothing-to-lint returns 2, not 0`)에 쉼표가 있어 **한 단계가 둘로 세어졌다.**
구분자는 데이터에 나올 수 없어야 한다 — 한 줄에 한 단계씩으로 바꿨다.

### 재귀 신호를 «불리언» 에서 «깊이» 로

가드레일 스위트를 프로브 안에서 통째로 건너뛰고 있었다. 그래서 「guardrail suite」 단계가
사각지대였다. 그렇다고 다 돌리면 스위트가 preflight 를 세 번 더 부르면서 21초가 **2분 3초**
가 됐다.

불리언은 「프로브가 이 스위트를 재고 있다」와 「이 스위트가 딴 걸 재느라 부른 실행이다」를
구별하지 못한다. 깊이는 구별한다. **깊이 1 = 프로브가 잰다(스위트 실행) · 깊이 2+ = 아무도
안 잰다(생략).** 여기에 중첩 실행의 조기 종료를 더해 **28초**가 됐다 — 8단계 전부를 재면서
원래(21초)와 비슷하다. 프로브만이 잡을 수 있는 6개 케이스는 중첩 안에서 「프로브가 꺼져
있어 증명 불가」로 명시하고 건너뛴다. 나머지 여든 몇 개는 그대로 돈다.

### CI 는 «필수 줄이 제자리인 채로» 안 돌 수 있었다

셋 다 validator 와 `actionlint` 를 함께 통과했다:

| 우회 | 왜 안 도나 |
|---|---|
| `needs: [precheck]` | 선행 job 이 skip 되면 이 job 도 skip |
| `env: {BASH_ENV: …}` | 그 파일에서 `exit 0` 하면 스텝이 시작되기 전에 성공 종료 |
| `types: [closed]` · `tags:` 만 · `branches: ["**", "!main"]` | 트리거가 아예 발화하지 않거나 보호 브랜치를 도로 뺀다 |

`container:`·`services:` 도 같은 계열이라 함께 막았다. 회귀 케이스 5개를 넣고 각각 결함
주입으로 잡히는 것을 확인했다.

반대로 **정상 표현 하나를 막고 있었다**: 게이트 스텝의 `shell: bash` 는 ubuntu 러너 기본값을
명시한 것뿐이라 이제 통과한다. `defaults:` 도 `run.shell` 이 없으면 통과한다.

### 자기시험이 사용자 파일을 지웠다

`scripts/.self-test-runner.py` 라는 **고정 경로**에 쓰고 무조건 지웠다. 같은 이름의 파일이
있으면 사라졌고, 병렬 실행끼리도 부딪혔다. 임시 디렉터리로 옮겼다.

### 문서가 코드보다 세게 말하고 있었다

- 「나머지 다섯도 초록인지 본다」 · 「패턴을 맞히려면 실제로 재는 수밖에 없다」 — 둘 다
  당시 코드보다 강한 주장이었다. 해당 절에 반증을 붙였다.
- 「저장소 파일이 환경변수를 만들 수 없다」 — **틀렸다.** 러너 코드·로컬 셸·러너 이미지가
  세팅할 수 있다.
- `OPEN-FINDINGS.md` 가 재귀 신호를 한 곳에선 «파일», 다른 곳에선 «환경변수» 라고 적고
  있었다. 그리고 **§D-7 은 참조만 있고 절이 없었다** — 매달린 참조다.
  이제 §C-13(신뢰 경계)·§C-14(환경변수 이름 목록)로 실제 절을 썼다.
- PyYAML 이 로컬에서도 필요한데 설치는 CI 에만 적혀 있었다. README·CONTRIBUTING 에 적었다.

### 못 닫은 것 — §C-13

프로브는 러너 «자신의 출력» 으로 판정한다. 그래서 러너를 다시 쓰면 통과한다: 프로브
환경에서는 「8/8 실패」를, 평상시에는 초록을 찍으면 된다(리뷰 14 실측 — 단계는 하나도 안
돌았다). **재는 도구가 재는 대상과 같은 레포에 있는 한 코드로는 못 닫는다.** 프로브가 막는
것은 «실수로 안 돌게 된 것» 이고, 못 막는 것은 «의도적 위조» 다. 이 세 파일의 변경은 diff 를
사람이 읽어야 한다.

## [2.10.3] - 2026-08-28

13차 리뷰. 세 가지가 통과했고, 셋 다 **관찰의 «해상도»** 문제였다.

### 결함 하나로는 부족하다 — 이제 패턴을 요구한다

프로브가 판정기 하나만 깨뜨리니, 러너가 이렇게 하면 통과했다:

```python
if label != "guardrail self-test":
    continue                 # 여덟 단계 중 일곱이 안 돌고 전부 초록
```

이제 프로브는 **서로 다른 세 단계에 결함을 심고**, 그 셋이 빨갛고 **나머지 다섯이
초록인지** 본다. 그리고 단계별 판정은 러너 «자신의 요약» 에서 읽는다 — 본문에서
"FAIL" 을 찾으면 단계마다 실패를 다르게 표현한다는 사실("Invalid JSON:")에 걸린다.

> ⚠️ **이 판은 부족했다.** 재지 않는 다섯 단계는 «실행을 생략해도» 초록이었다.
> 그리고 판정을 러너 출력에서 읽으므로 러너를 위조하면 통과한다. 다음 판(v2.11.0)이
> 여덟 단계 전부로 넓혔고, 위조는 못 막는 한계로 `docs/OPEN-FINDINGS.md` §C-13 에 적었다.

### 프로브 신호가 «파일» 이면 CI 가 끌 수 있다

`.guardrail-nested` 는 앞 단계에서 `touch` 한 줄이면 프로브를 통째로 껐다.
신호를 **환경변수**로 바꿨고, CI 가 그 이름을 언급하는 것을 게이트가 막는다. 옛 파일이
남아 있으면 잔재로 지적한다.

> ⚠️ 「저장소 파일이 환경변수를 만들 수 없다」고 썼는데 **틀렸다** — 러너 코드·로컬 셸·
> 러너 이미지가 세팅할 수 있다. 파일보다 나은 신호이지 «만들 수 없는» 신호가 아니다
> (`docs/OPEN-FINDINGS.md` §C-14).

### CI 는 이제 YAML 을 «파싱» 한다

네 판 연속 텍스트로 틀렸다 — 머리말 주석 · 액션 입력의 `run:` · `>` 접기가 만든 셸
주석 · 그리고 이번엔 **job `name: |2` 블록 안의 가짜 `- name:`** 이 「단계 자리」를
흉내 냈다. **텍스트 위치는 YAML 구조가 아니다.** PyYAML 로 읽고,
- 어느 job 이 그 두 명령을 «단계로» 돌리는지
- 그 job 이 조건부인지 · `continue-on-error` 인지 · `defaults` 가 걸리는지
- 트리거가 실제로 발화하는지(`branches` 를 없는 브랜치로 좁히거나 `paths-ignore` 로 전부
  빼면 모든 줄이 제자리인 채 아무것도 안 돈다)
를 직접 묻는다. 파서가 없으면 **실패로 닫는다**(CI 가 설치한다).

🔑 부수 효과: **다른 job 은 `if:`·`shell:` 을 마음대로 쓸 수 있다.** 텍스트 판정 때는
전역 금지였고, 그건 리뷰가 지적한 과잉이었다.

### 코드 품질

- 자기시험이 **운영 파서를 직접 부른다**(사본을 시험하면 시험한 적이 없다)
- 프로브에 **timeout 600초** — 무한 루프가 CI 한도까지 매달리지 않게
- 러너가 단계 예외를 **그 단계의 실패로 집계**한다(traceback 으로 중단하지 않는다)
- 미사용 import 정리(`re`·`shlex`)

### 이 판의 증명

저장소 14규칙(**47 케이스**) + 린트 9규칙(19 케이스) + 유효변형 8 + 강제확인 2 +
선언·실행 대조 1 + 면제제안 1. 새 4종 전부 구현을 되돌려 `did NOT fail` 을 확인했다.

## [2.10.2] - 2026-08-28

12차 리뷰의 핵심은 개별 결함이 아니라 한 문장이었다:
**「소스 모양을 고정하면서도 의미적 우회를 허용한다 — 과하다.」** 맞는 말이고, 고쳤다.

### 모양 규칙 넷을 «행동 관찰» 하나로 바꿨다

`main()` 이 STAGES 를 돌고 · `action()` 을 부르고 · `subprocess.run` 하고 · `return 1`
한다 — 이 넷을 소스에서 찾던 규칙은 **양방향으로 틀렸다**:

```python
if False:
    action(); subprocess.run(action); return 1   # 표식 다 있고 실행은 0회
```

그리고 정상 리팩터링(변수명 변경 · 헬퍼 추출 · `check_call` · `return bool(failed)`)을
거부했다. **소스 텍스트는 행동이 아니다.**

이제 게이트가 **판정기를 하나 망가뜨린 사본에서 진입점을 실제로 돌리고**, 그 단계에서
빨개지는지 본다(`_preflight_actually_enforces`). `main()` 을 비우든, 별칭으로 `STAGES` 를
비우든, argv 를 딴 데로 돌리든 — 전부 여기서 걸린다. **소스가 어떻게 생겨야 한다는 규칙은
한 줄도 없다.**

같은 이유로 STAGES 리더도 느슨해졌다: 「어떤 게이트가 선언됐나」만 답하고, `PY` 로
시작해야 한다·람다 금지·선언 후 변형 금지 같은 규칙은 **뺐다**(전부 별칭 한 줄로
우회됐고, 정상 작업을 막고 있었다).

### CI 에서 «줄은 제자리인데 안 도는» 세 가지

| | 이제 |
|---|---|
| `defaults: run: shell: true {0}` — 모든 `run:` 이 성공하는 no-op | `defaults:`·`shell:` 금지 |
| `"if" : …` — 따옴표와 공백으로 접두사 검사 우회 | 키를 정규식으로 판정 |
| `on:` 을 `workflow_dispatch` 만 남김 | `pull_request:`·`push:` 트리거를 요구 |

### 🔴 없는 결함을 막던 규칙을 뺐다

11차 리뷰가 「주석 끝의 `\` 가 다음 줄을 먹어 `exec` 가 사라진다」고 했고, 나는 **재현하지
않고 규칙을 넣었다.** 이번에 직접 재 보니 bash 는 주석 안의 백슬래시를 잇지 않는다
(`# c \` + 개행 + `echo RAN` → **RAN 출력**). 규칙과 그 케이스를 지웠다 —
그 케이스는 **틀린 이유로 통과**하고 있었다.

반면 U+0085 는 진짜다(파이썬은 줄로 보고 bash 는 안 본다). 다만 그 입력은 **아무것도 안
돌기 때문에 행동 프로브가 잡는다** — 정적 거부 규칙은 없앴다.

🔑 **리뷰어의 지적도 전제를 재 봐야 한다.** 「고쳤다」 전에 「그게 정말 결함인가」다.

### 빨라졌다

프로브 안에서는 가드레일 «스위트» 를 건너뛴다(프로브가 재는 건 1단계뿐이다).
그리고 프로브는 **정적 검사가 아무 말도 안 했을 때만** 돈다.
preflight **50초 → 20초**, `--only preflight-stages` **7초 → 0.7초**.

### 이 판의 증명

저장소 14규칙(**43 케이스**) + 린트 9규칙(19 케이스) + 유효변형 8 + 강제확인 2 +
선언·실행 대조 1 + 면제제안 1.

## [2.10.1] - 2026-08-28

11차 리뷰. 구조를 바꿨다고 끝난 게 아니었다 — **선언을 읽는 것은 프로그램을 읽는 것이
아니다.** 세 가지가 전부 통과했고, 정적 검사는 하나도 안 울었다.

| 통과하던 것 | 왜 |
|---|---|
| `main()` 을 «라벨만 찍고 return 0» 으로 | `STAGES` 는 멀쩡하다. 게이트가 **엔진을 안 봤다** |
| `STAGES[:] = [(라벨, lambda: True) …]` | 라벨이 그대로라 「선언 vs 실행」 대조도 못 잡는다 |
| 주석 끝의 `\` 하나 | bash 가 줄 연결을 지워 `exec` 가 주석에 먹힌다. 줄 단위로 보면 멀쩡 |
| 진짜 단계를 `echo` 로 바꾸고 정규 두 줄을 job `env:` 블록에 | 개수도 순서도 맞다 |
| `if: ${{ false }}` 를 job 에 | 모든 필수 줄이 제자리에 있는데 아무것도 안 돈다 |

### 고친 것

- **`main()` 의 «모양» 을 ast 로 본다** — STAGES 를 도는 루프 · `action()` 호출 ·
  `subprocess.run` · 실패 시 `return 1`. 하나라도 없으면 거부한다.
- **선언 이후의 `STAGES` 변형을 금지**한다(`clear` · 슬라이스 대입 · `+=` · 재대입).
  그리고 **모르는 모양은 버리지 않고 거부**한다 — 예전 리더는 `ast.Constant` 만 남기고
  나머지를 조용히 버려서 `[NOOP, "…"]` 를 정상 명령으로, `lambda` 를 정상 단계로 읽었다.
- preflight.sh 의 «유효 줄» 을 세기 전에 **bash 처럼 줄 연결을 먼저 지운다**.
- CI 의 필수 줄은 **«단계 자리»** 에 있어야 한다(바로 위가 `- name:`). 그리고
  `if:` · `continue-on-error:` 를 금지한다 — 줄을 지우지 않고 단계를 끄는 방법이다.

### 지운 케이스 하나

`ci-preflight-decoy`(진짜 단계는 두고 사본만 추가)는 **더는 결함이 아니다** — 자리로
판정하니 사본이 있어도 진짜 단계가 돈다. 케이스가 안 울어서 지운 게 아니라, **그 입력이
정상이 됐기 때문에** 지웠다. 위험한 변형(진짜 단계를 «대체»)은 새 케이스가 맡는다.

### 이 판의 증명

저장소 14규칙(**38 케이스**) + 린트 9규칙(19 케이스) + 유효변형 8 + 강제확인 2 +
선언·실행 대조 1 + 면제제안 1. **새 6종 전부 구현을 되돌려 `did NOT fail` 을 확인**했다.

## [2.10.0] - 2026-08-28

**열 번째 리뷰가 접근 자체를 기각했고, 동의한다.** `eval 'run() { …; }'` 한 줄로
다섯 게이트가 통째로 안 돌았는데 preflight 도 무결성 확인도 초록이었다. `readonly fail`,
`builtin trap` 도 마찬가지였다. **정규식 금지목록으로는 닫히지 않는다** — 셸이 무언가를
덮어쓰는 방법의 목록은 끝나지 않는다.

### 진입점을 선언적 파이썬 러너로 (§D-9)

`scripts/preflight.sh` 는 이제 **두 줄**이다.

```bash
#!/usr/bin/env bash
exec python3 "$(dirname "$0")/preflight_runner.py" "$@"
```

단계는 `scripts/preflight_runner.py` 의 `STAGES` **데이터**에 있다. 개수는
`len(STAGES)` 에서 파생되고(손으로 맞추는 `STAGES_EXPECTED` 는 사라졌다), 종료 코드를
정하는 자리는 한 곳뿐이다. 게이트는 셸을 «해석» 하지 않고 **구문 트리를 읽는다**.

그래서 다음이 전부 필요 없어졌고, 지웠다: 실행기 본문 고정 · 정의 1회 검사 ·
함수 허용목록 · 줄 연결 결합 · `exit $fail` 마지막 줄 · 리터럴 `exit` 금지 ·
`fail=0` 1회 · `trap` 금지 · `STAGES_EXPECTED`. **여덟 개의 금지 규칙이 «두 줄 고정»
하나로 접혔다.**

### CI 판정도 YAML 해석을 버렸다

세 판 연속 틀렸다 — 파일 전체 문자열이 머리말 주석을 셌고, `run:` 이름 매칭이 액션의
`with: run:` 입력을 셌고, `>` 접기가 명령을 셸 주석으로 만드는 것을 놓쳤다.
이제 **우리가 쓴 파일의 줄을 고정**한다: 두 줄이 각각 정확히 한 번, 이 순서로.
접는 스칼라 안에 숨긴 미끼는 «두 번» 이 되어 걸린다.

### 지운 줄이 더 많다

셸/YAML 파서 **57줄 삭제**. `_invokes`·`_statement_invokes`·`_unconditional_lines`·
`_workflow_run_commands`·`_strip_shell_comments`·`_canonical` 전부 없다.
자기시험은 이제 «러너의 단계 선언을 옳게 읽는가» 를 시험한다.

### 선언과 실행을 맞대 본다

구조를 바꾸고 나서 스스로 뚫어 봤다: `STAGES` 는 그대로 두고 `STAGES.pop(0)` 한 줄을
넣으면 **정적 게이트는 통과하는데 그 단계가 안 돈다.** 그래서 가드레일이 깨끗한 사본에서
preflight 를 한 번 돌려, **선언된 라벨 목록과 실제로 찍힌 `== ` 헤더 목록이 같은지**
(순서까지) 대조한다. 주입으로 확인했다 — 정적 게이트 rc=0, 교차확인은 잡는다.

### 이 판의 증명

저장소 14규칙(**33 케이스**) + 린트 9규칙(19 케이스) + 유효변형 8 +
**preflight 가 «그 단계에서» 빨개지는 것 2건** + **게이트가 제안한 면제가 통하는지 1건**.
새 케이스 5종 전부 구현을 되돌려 `did NOT fail` 을 확인했다.

⚠️ 승인은 아직이다. 열 번 중 판정이 나온 아홉 번 모두 지적이 있었다.

## [2.9.4] - 2026-08-28

9차 리뷰. 「셸을 파싱해서 «이게 실행되는가» 를 맞힌다」는 접근이 **네 번째로 뚫렸고**,
이번엔 그 접근 자체를 버렸다.

### `_invokes` 를 지웠다 — 계약을 좁히는 편이 옳다

`bash -c ':' scripts/preflight.sh` 는 스크립트를 실행하지 않는데 통과했고,
`bash -O extglob X` · `bash -p X` 는 실행하는데 거부됐다. 리뷰어가 8·9차에 걸쳐 같은
방향을 두 번 권고했고 나도 동의한다 — **셸 문법은 유한한 목록이 아니다.**

CI 는 이제 정확히 이 두 줄을 돈다. 파싱이 아니라 글자 대조다.

```
python3 scripts/validate_repository.py --only preflight-stages
bash scripts/preflight.sh
```

`|| true` · `make` · `npm run` · `working-directory` 변경 · `docker exec` 는 전부
**거부**된다. 못 잡는 결함이 아니라 명시한 계약이다(`docs/OPEN-FINDINGS.md` §C-12).
preflight 가 부르는 명령도 같은 방식으로 못박았다.

### 나머지 다섯 건

| 통과하던 것 | 이제 |
|---|---|
| `r\`+개행+`un() {…}` — bash 는 줄 연결을 파싱 전에 지운다 | 우리도 먼저 결합하고 센다 |
| `python3 ()` 다음 줄에 `(` 본문 | 정의 문법을 열거하는 대신 **허용 목록**(`run`·`stage`)으로 뒤집었다 |
| `PREFLIGHT_ENFORCEMENT.clear()` 한 줄 | 정적 개수 + **실행 시점에 다시 센다** |
| `export OH_MY_HARNESS_GUARDRAIL_NEST=1` (스크립트 안·composite action·`$GITHUB_ENV`) | 환경변수를 없애고 **사본 안의 파일**로. 추적되면 게이트가 거부 |
| allowlist 오류 메시지가 그대로 붙여도 안 통했다 | `sha` 에 `#N` 을 붙여 알려주고, **붙여 넣으면 실제로 통과하는지** 가드레일이 매번 확인한다 |

리뷰가 함께 지적한 단일 파일 우회 둘도 닫았다: **`fail=0` 재설정**(1회만, 단계 앞에서만)과
**`trap`**(금지). 둘 다 「단계는 다 돌았는데 결과만 거짓」이다.

### 훅 설치가 남의 훅을 지우고 있었다

「내용에 preflight 가 있으면 우리 것」으로 판단해서, preflight 와 **다른 검사도 함께**
돌리는 훅이 백업 없이 사라졌다. 이제 쓸 내용과 **바이트를 대조**해 다르면 백업한다.

### 리뷰를 기다리는 동안 파서에서 하나 더 찾았다

`run:` 이라는 «이름» 만 보고 단계를 셌더니, 액션의 입력으로 들어간
`with:\n  run: bash scripts/preflight.sh` 가 요구 목록을 만족시켰다 — CI 는 아무것도
실행하지 않는데. `run:` 은 **그 단계 자신의 키일 때만** 단계다. 들여쓰기로 판정한다.
(`>` 접는 스칼라 · `|2` · 탭 들여쓰기 · 앵커 참조도 함께 확인했다.)

### 문서가 코드보다 뒤처져 있었다

`docs/OPEN-FINDINGS.md` 가 자기시험 건수를 낡은 값으로 적고 있었고, v2.9.3 의
「전부 고쳤다」는 과장이었다. 둘 다 고쳤다.

### 이 판의 증명

저장소 14규칙(**40 케이스**) + 린트 9규칙(19 케이스) + 유효변형 8 +
**preflight 가 «그 단계에서» 빨개지는 것 2건** + **게이트가 제안한 면제가 실제로 통하는지 1건**.
이번 8종 전부 구현을 되돌려 `did NOT fail` 을 확인했고, 새 절도 옛 메시지로 되돌려
실패하는 것을 확인했다. preflight 40초.

🔑 이 판에서 처음으로 «규칙이 발화하는가» 가 아니라 **«규칙이 알려준 해결책이 실제로
통하는가»** 를 검사에 넣었다. 통하지 않는 해결책을 알려주는 게이트는 사람이 끈다.

⚠️ **아직 승인을 못 받았다.** 아홉 번 중 판정이 나온 여덟 번 모두 지적이 있었다.
`docs/OPEN-FINDINGS.md` §D-9(진입점을 파이썬 선언적 러너로)는 다음 판의 첫 작업이다.

## [2.9.3] - 2026-08-28

**8차 리뷰가 처음으로 판정을 냈다**(7차는 공급자 콘텐츠 필터로 중단). 지적 다섯 건이
전부 실재했고, 전부 「검사가 «있나» 를 물었지 «그런가» 를 묻지 않았다」였다.

| 지적 | 통과하던 것 |
|---|---|
| `exit $fail` 을 **존재**로만 확인 | 단계들 «위» 에 `exit 0` 을 놓고 진짜 줄은 아래 그대로 두면 통과 |
| 정의를 `name()` 문법으로만 인식 | bash 의 `function run { … }` 로 no-op 을 덮어쓰면 통과 |
| enforcement 절이 «불리는지» 만 확인 | `PREFLIGHT_ENFORCEMENT = []` 로 비우면 돌긴 도는데 아무것도 안 잰다 |
| nest 스위치를 `run:` 에서만 탐색 | job-level `env:` 로 켜면 통과 |
| allowlist 키 = path+token+line-hash | 같은 파일에 **글자까지 같은 줄**이 둘이면 하나만 승인해도 나머지가 따라 들어옴 |

다섯 건을 고치고 케이스 5종을 넣어 각각 구현을 되돌려 `did NOT fail` 을 확인했다.
⚠️ 「전부 고쳤다」고 썼던 문장을 v2.9.4 에서 내렸다 — 바로 다음 리뷰가 같은 자리에서
여섯 건을 더 찾았다. 「그 지적들을 고쳤다」가 「그 부류를 닫았다」는 아니다.

### `_invokes` 반대 방향 반례 셋도 받았다

`bash -euo pipefail X`(묶음 옵션의 `o` 가 값을 먹는다) · `bash -c 'bash X'`(값 안을 다시
본다) · `( bash X )`(서브셸). 전부 정상 CI 표현인데 거부하고 있었다. 자기시험 29 → **32건**.

⚠️ 아직 «실행 아님» 으로 읽는 정상 표현이 남아 있다 — `make`/`just`/`npm run`(manifest 를
읽어야 안다) · `working-directory` 가 바뀐 스텝 · `docker exec`. 리뷰어 제안대로 **계약을
「레포 루트에서 직접 호출」로 좁혔다고 문서에 명시**한다. 규칙을 넓히는 대신 계약을 좁혔다.

### 강제 확인이 «그 단계» 에서 빨개졌는지까지 본다

전에는 「어디서든 non-zero」면 증거로 셌다. 무관한 실패가 그 케이스를 영원히 통과시킬 수
있다. 이제 해당 단계 구간을 잘라 그 안에서 실패했는지 확인한다.

### 그 밖

- 추적되는 파일은 이름이 `build/` 든 **우리 것**이다 — 이름 기반 제외는 추적 안 되는 것에만
- CI 무결성 스텝을 preflight **앞**으로(문서와 실제가 어긋나 있었다) · 판정도 «같은 문장» 안에서
- `install-hooks.sh` 가 남의 pre-push 훅을 **백업 없이 덮어쓰던 것** 수정
- 사본에서 `.omc`/`.omx` 제외 + 깨끗한 트리 검사 게이트당 1회 캐시

### 이 판의 증명

저장소 14규칙(**35 케이스**) + 린트 9규칙(19 케이스) + 유효변형 8 +
**preflight 가 «그 단계에서» 빨개지는 것 2건**. 이번 5종을 포함해 **최근 15종 전부 구현을
되돌려 `did NOT fail` 을 확인**했다. 자기시험: 가드레일 5 · 검사기 **32**. preflight 38초.

## [2.9.2] - 2026-08-28

2.9.1 을 내보내고 **내가 직접 그 수정을 뚫어 봤다. 세 번 뚫렸다.** 전부 정적 고정
(`PREFLIGHT_RUNNER_BODIES`)이 「본 적 있는 모양」만 막고 있었기 때문이다.

| 뚫은 방법 | 왜 통과했나 |
|---|---|
| `run()` 을 **한 번 더** 정의 | 고정이 «첫 줄» 만 봤다. 셸은 나중 정의를 쓴다 |
| `python3() { return 0; }` 삽입 | 실행자 이름을 셸 함수로 가리면 그 단계가 no-op |
| `exit $fail` → `exit 0` | 모든 게이트가 빨개져도 스크립트가 성공을 보고 |

### 그래서 «본문 고정» 대신 «실제로 돌린다»

가드레일이 판정기를 하나 망가뜨린 사본에서 **`bash scripts/preflight.sh` 를 진짜로
실행하고 non-zero 를 요구**한다. 중복 정의든 셰도잉이든 경로가 무엇이든 답은 같다.
재귀는 `OH_MY_HARNESS_GUARDRAIL_NEST` 로 끊고, **CI 가 그 변수를 쓰는 것을 게이트가
금지**한다(끄는 스위치이므로).

정적 고정은 남기되 셋을 더 본다: **정의가 하나인지 · 실행자 이름의 셸 함수가 없는지 ·
`exit $fail` 로 끝나는지.**

### `exit 0` 은 스크립트 «안에서는» 원리적으로 못 잡는다

그래서 CI 와 pre-push 훅이 **preflight 의 종료코드를 믿지 않는 한 줄**을 먼저 돌린다:
`python3 scripts/validate_repository.py --only preflight-stages`.
게이트 목록의 재나열이 아니라 방금 돌릴 스크립트의 무결성 확인이고,
`ci-runs-preflight` 가 그 줄을 **요구**한다.

### `_invokes` 는 제어흐름을 몰랐다

`if false; then bash scripts/preflight.sh; fi` · 히어독 안의 같은 문장 · 아무도 안 부르는
함수 몸통이 전부 「실행」으로 셈해졌고, `bash -eu -o pipefail scripts/preflight.sh` 는
`pipefail` 이 스크립트 자리로 보여 거부됐다. 무조건 실행되는 줄만 남기되 **불리는 함수의
몸통은 포함**한다(전부 버리면 멀쩡한 CI 를 거부한다). 자기시험 17 → **29건**.

### 가드레일 절 자체를 지우면?

`guardrail_preflight_enforces(...)` 한 줄만 지우면 그 절이 통째로 사라지고 요약 문장만
짧아진다. 이제 밖에서 센다(`REQUIRED_GUARDRAIL_SECTIONS`).

### 이 판의 증명

저장소 14규칙(**30 케이스**) + 린트 9규칙(19 케이스) + 유효변형 8 +
**preflight 가 실제로 빨개지는 것 2건**. 새 케이스 6종을 포함해 **10종 전부 구현을
되돌려 `did NOT fail` 을 확인**했다. 판정 함수 자기시험: 가드레일 5 · 검사기 **29**.

⚠️ 7차 리뷰는 판정을 못 냈다 — 결함 주입 프롬프트가 공급자 콘텐츠 필터에 걸려 중단됐다
(`exit 1`). 그 리뷰의 **사전 가설**은 남았고, 위 수정의 절반은 그 가설과 독립적으로
내가 재현한 것이다. **승인 없이 배포하지 않는다** — 8차를 다시 돌린다.

## [2.9.1] - 2026-08-28

여섯 번째 리뷰. 앞선 다섯 번이 「검사를 지우면 아무도 모른다」를 고쳤다면, 이번엔
**「검사를 «부르는 줄» 은 그대로 두고 안 돌게 만들 수 있다」** 였다.

### 실행기를 고정한다 — 호출 줄이 아니라 실행

`run()` 을 「`--self-test` 면 그냥 return 0」 으로 고치면 호출 줄도 `STAGES_EXPECTED`
도 그대로인데 그 단계가 한 번도 안 돈다. 실측으로 `preflight-stages` 와 `preflight`
둘 다 초록이었다. 이제 게이트가 **실행기 본문을 글자 그대로 고정**한다.
스크립트는 자기 편집을 막지 못하므로 판정은 바깥 파일이 한다 — 남는 한계는
`docs/OPEN-FINDINGS.md` **§D-7**.

### `_invokes` 는 양방향으로 틀려 있었다

| 통과하면 안 되는데 통과 | 거부하면 안 되는데 거부 |
|---|---|
| `bash -n scripts/preflight.sh` (문법검사) | `/bin/bash scripts/preflight.sh` |
| `command -v scripts/preflight.sh` (경로조회) | `sudo -u root bash scripts/preflight.sh` |
| `false && bash scripts/preflight.sh \|\| true` (죽은 분기) | 따옴표 친 경로 |

argv 위치를 파싱하고, 실행자는 basename 으로 보고, 죽은 분기를 표시한다.
그리고 **`python3 scripts/validate_repository.py --self-test` 가 양방향 17건을 고정**한다
— 이 스크립트도 이제 자기 판정 함수를 시험한다(preflight 두 번째 단계).

### 「지워도 초록」인 규칙 넷에 전용 케이스

`_invokes` 를 부분문자열로 되돌려도, stale allowlist 검사를 통째로 지워도 가드레일이
초록이었다. 새 케이스 4종을 넣고 **각각 구현을 되돌려 `did NOT fail` 로 죽는지 확인**했다.

- `preflight-run-tampered` — 단계를 세면서 실행은 건너뛰는 실행기
- `ci-preflight-echo-only` — `run: echo scripts/preflight.sh`
- `dead-api-stale-allowlist` — 존재하지 않는 줄을 면제하는 항목
- `dead-api-build-dir` — 우리가 쓰는 `docs/build/` 안의 제거된 API

### `dead-api` 범위 — 이름이 아니라 위치

`vendor`/`dist`/`build` 를 «경로 조각 어디에나» 적용해 `docs/build/` 가 통째로 스캔
밖이었다. **최상위일 때만** 제외한다.

### 검사기 A — 자기시험이 조건부였다

합성 스텁 시험이 `if codex 있음` 블록 안에 있어서, 파서를 깨뜨리면 「codex 없음」을
찍고 **아무것도 안 잰 채 exit 0** 이었다. 무조건 실행으로 옮겨 자기시험이 4건 → **19건**.
산문 케이스도 구간 «밖» 으로 옮겼다 — 안에 있을 때는 구간 분리가 아니라 「모르는 줄」
규칙으로 통과하고 있었다(틀린 이유로 통과, 오늘 일곱 번째).

### 이 판의 증명

저장소 14규칙(**24 케이스**) + 린트 9규칙(**19 케이스**) + 유효변형 8.
새 케이스 4종은 **구현 되돌리기로 load-bearing 임을 확인**했다.
판정기 자기시험: 가드레일 5건 · **검사기(validate_repository) 17건** · A 19건 · B 17건.

## [2.9.0] - 2026-08-28

다섯 번째 리뷰가 개별 버그가 아니라 **부류**에 이름을 붙였다: **「검사를 지우면 아무도
모른다」.** self-test 호출 줄을 지우고 판정기까지 깨뜨려도 preflight 가 exit 0 이었다 —
게이트를 세워 놓고 **그 게이트를 부르는 줄** 은 아무도 지키지 않았다.

### 배선을 두 겹으로 지킨다

- preflight 가 **자기 단계 수를 센다**(`STAGES_EXPECTED`). 한 줄 지우면 개수가 어긋난다
- `preflight-stages` 게이트가 preflight 의 `run` 줄을 **파싱해** 각 검사기가 실제 실행
  자리에 있는지 본다. 제거·바꿔치기·주석화 셋 다 잡는다

### 🔴 `dead-api` — 「지시문인지 알아맞히기」를 그만뒀다

자문 둘이 **독립적으로 같은 답**을 냈다. 부정어 목록으로 지시문과 서술을 가르는 건
의미 분석 문제라 목록과 정규식으로 닫히지 않는다. 그래서 규칙을 불변식으로 바꿨다:

> 제거된 API 식별자는 저장소에 원칙적으로 없어야 한다. 필요한 등장은 **그 occurrence 에**
> 이유를 명시해야 한다.

- **스캔 경계를 확장자가 아니라 소유권으로**: git 이 아는 first-party text 전부.
  `.sh`·`.py`·`.json`·`.ts` 가 안 보이던 사각지대가 닫혔다
- **면제 키 = 경로 + 토큰 + 그 줄의 해시 + 사유**(`docs/dead-api-allowlist.json`, 115건).
  줄을 고치면 면제가 stale 이 되어 다시 묻는다 — 사유는 옛 문장에 대해 쓴 것이다
- 파일 전체 면제 3개, `tests/` 서브트리 통째 제외, 대소문자 구분 — 전부 제거
- 거짓양성도 닫혔다: `TeamCreate was the original coordination API.` 같은 중립 서술은
  이제 **사유를 적으면** 통과한다. 규칙이 의도를 추측하지 않는다
- git 이 아무것도 못 돌려주면 「전부 깨끗」이 아니라 **「무엇이 우리 것인지 모른다」로 실패**

### `orphan-agents` — 표에 이름 적는 건 배선이 아니다

균형 잡힌 `Agent(...)`/`Task(...)` **안의** 리터럴만 호출로 인정한다. 백틱·`@name`·
`agent: "name"` 예제·표의 `subagent_type` 넷 다 이제 잡힌다. 언급 횟수는 버리지 않고
**오류 메시지**에 쓴다 — 「4번 언급됐지만 한 번도 안 불림」이 고치기 쉽다.

### 스킬 도달 — 구간을 나눠 읽는다

`### Available skills` 구간 «안에서만» 항목을 읽고, 그 구간의 **모르는 줄은 조용히
무시하지 않고 «형식 변경»(exit 2)** 으로 처리한다. 중복 등록도 통과시키지 않는다.
⚠️ 자문이 확인해 준 한계: **정확한 슬롯의 동일한 바이트는 이 계층에서 구별할 수 없다.**
항목별 provenance 가 없기 때문이다 — `docs/OPEN-FINDINGS.md` **§A-13** 에 적었다.
(2.9.0 은 「적었다」고만 썼고 실제로는 없었다. 2.9.1 에서 실제로 적었다.)

### 🔴 그 과정에서 내가 낸 결함 셋

- **`.omx/` 가 `.git/info/exclude` 에만 있었다** — 로컬 전용이라 내 기계에서만 제외되고
  신선 클론에서는 스캔에 들어왔다. 추적되는 `.gitignore` 로 옮겼다
- **새 게이트가 거짓양성을 냈다** — 「실행자」 목록에 `python3` 를 빠뜨려 멀쩡한 preflight 를
  「아무것도 안 부른다」고 4건 보고했다. **규칙을 좁힐 때는 정상 입력도 같이 재라**
- **자기시험 스텁이 닫는 태그를 빠뜨렸다** — 그래서 「형식 변경」 케이스들이 전부
  **틀린 이유로** 통과하고 있었다. 오늘 다섯 번째 같은 병이다

### 이 판의 증명

저장소 14규칙(**20 케이스**) + 린트 9규칙(**19 케이스**) + 유효변형 8.
`orphan-agents` 는 구현을 되돌리는 주입으로 **케이스가 실제로 구현에 의존함**을 확인했다.
검사기 A 자기시험 **19건** · B **17건** · 판정기 5건.

### 첫 파일럿

`docs/pilot-2026-08-27-ppppcorp.md` — 서버 인프라 진단 하네스를 실제로 생성해 돌렸다.
린터 0건, 세 시나리오 라우팅 정확, 생성 에이전트 3개 전부 실제 호출됨. 그리고 그 에이전트들이
**이 저장소의 결함을 찾았다**(내 미측정 주장, dead-api 사각지대 5, cron 되돌림 만료).

## [2.8.4] - 2026-08-27

네 번째 승인 리뷰도 **REJECTED**. 이번엔 HIGH 아홉 건이었고, 그중 가장 나쁜 것은
**자기시험이 게이트에 연결돼 있지 않았다**는 것이다.

### 🔴 자기시험이 강제 경로 밖에 있었다

`verdict()` 의 exit-2 보호를 지우면 `--self-test` 는 실패하는데 **preflight 는 통과**했다.
판정기를 시험하는 장치를 만들어 놓고 아무도 부르지 않는 자리에 뒀다. 이제 preflight 의
**첫 단계**가 판정기 자기시험이고, 주간 감사도 A·B 자기시험을 먼저 돌린다.

### 🔴 같은 실수 다섯 번째 — 구조 대신 부분문자열

- **스킬 도달**: 줄 앵커를 걸었는데도 «항목처럼 생긴 설명 bullet» 이 통과했다
  (`- example: … (file: r1/alpha/SKILL.md)`). 이제 **선언한 이름과 경로의 디렉터리가
  같아야** 항목으로 센다
- **CI 게이트**: `run:` 안의 **셸 주석**만으로 통과했다. `run: |` 아래 주석만 있는
  블록도 마찬가지였다. run 본문에서 `#` 주석을 벗기고, 실행되는 것이 없으면 위반이다

### 🔴 사용자 문서와 계약

- 영어 README 는 여전히 **"Seven deterministic rules"** 였다. 규칙 «이름» 만 검사하고
  개수를 말하는 문장은 안 봤다 — 이제 규칙표 도입 문장의 숫자도 검사한다
- **`dead-api` 가 워크플로우를 안 봤다.** `orphan-agents` 는 읽도록 고쳤는데 이쪽은
  빼먹어서, `TeamCreate({team_name:...})` 이 v2 주력 실행 경로에 있는데 clean 이었다

### 🔴 계약 위반 두 건 (스킬 도달)

- 프롬프트 **형식이 바뀌면** `exit 1`(미도달)로 나갔다. 계약은 `2`(검사 불가)다 —
  「팩이 사라졌다」와 구분이 안 됐다
- **팩이 전부 사라지면** 조기 `exit 2` 로 조용히 끝났다. 최악의 경우가 가장 조용한
  경우였다. 기대 목록이 있으면 이제 「전부 사라짐」 위반이다

### 🔴 패리티 허용쌍에 방향이 없었다

런타임 문장쌍을 양쪽 모두에 치환해서, 두 문장을 **서로 뒤바꿔도** 통과했다
(Claude 파일이 "Codex session" 이라 말하고 그 반대도). 이제 왼쪽은 claude 쪽에만,
오른쪽은 codex 쪽에만 적용한다.

### 🔴 회귀 케이스 두 개가 또 헛돌았다

README 규칙명 검사와 워크플로우 폴백 제외는 **구현을 지워도 가드레일이 통과**했다.
- README 케이스는 «개수» 로 발화해서 «이름» 검사를 증명하지 못했다 → 전용 케이스 신설
- 템플릿 리터럴 픽스처가 `` `starting billing-ghost sweep` `` 이라 폴백 정규식
  (`` `이름` `` 정확 일치)에 애초에 안 걸렸다 → `` `billing-ghost` `` 로 교정

**「틀린 이유로 통과하는 테스트」가 오늘 세 번째다.**

### 이 판의 증명

저장소 13규칙(**18케이스**) + 린트 9규칙(**15케이스**) + 유효변형 8.
검사기 A 자기시험 **17건** · B **17건** · 판정기 **5건**.
⚠️ v2.8.3 이 「A 자기시험 17건」이라 적었을 때 실제로는 16건이었다. 지금은 17건이 맞다.

## [2.8.3] - 2026-08-27

세 번째 승인 리뷰도 **REJECTED**. 세 건이 남아 있었고 하나는 **네 번째 「구조 대신
부분문자열」** 이다. 이쯤 되면 개별 버그가 아니라 내 습관이다.

### 🔴 설명문 안의 «예시 한 줄» 이 스킬 등록으로 세어졌다

`<skills_instructions>` 블록을 **통째로** 정규식으로 훑고 있었다. 그래서
「항목은 이렇게 생겼습니다: `- example: … (file: r1/foo/SKILL.md)`」 같은 설명 문장 하나로
`foo` 가 등록된 것이 됐다. 레지스트리 항목은 줄 하나이므로 **줄 시작·줄 끝에 앵커**를 건다.
실물 100개 항목 전부 이 형식에 부합하는 것을 확인하고 바꿨다.

### 🔴 틸드 펜스 회귀 테스트가 아무것도 증명하지 않고 있었다

`~~~` 지원을 통째로 제거해도 가드레일이 통과했다. 픽스처가 가짜 제목을 **4개** 넣는데
계약이 **5개**를 요구하도록 바뀌어, 틸드를 모르는 검사기도 「4 < 5」로 발화했기 때문이다.
**틀린 이유로 통과하는 케이스**였다 — v2.8.0 의 D-6 과 같은 모양이 다시 나왔다.

가짜 제목 수를 린터의 상수에서 읽어 오게 했다. 이제 틸드 지원을 빼면 가드레일이 잡는다.

### 🔴 README 3종이 사용자에게 «7규칙·4섹션» 이라고 말하고 있었다

`lint-rule-docs` 는 자기가 쓰여진 문서 두 개만 보고 clean 을 냈다. 정작 **설치를 결정하는
사람이 읽는 문서**는 안 봤다. 이제 README 세 개도 검사한다:

- 구현된 9규칙이 전부 이름으로 나오는가
- `agent-sections` 행이 말하는 섹션 수가 실제 상수와 같은가

⚠️ 첫 구현은 `\b(\d+)\b` 를 썼는데 **한국어에서 `5개` 의 경계가 안 잡혀** 「숫자 없음」으로
읽었다. 한 언어에서만 틀리는 검사는 곧 꺼지는 검사다. 경계 규칙을 고치고 세 README 모두
숫자로 표기하게 통일했다.

### 이 판의 증명

저장소 13규칙(**16케이스**) + 린트 9규칙(14케이스) + 유효변형 8.
검사기 A 자기시험 **17건**.

## [2.8.2] - 2026-08-27

승인 리뷰가 **REJECTED** 하며 두 건을 결함 주입으로 재현했다. 둘 다 맞았고, 둘 다
**같은 실수의 반복**이다 — 구조를 봐야 할 자리에서 부분문자열을 봤다. 오늘 세 번째다.

### 🔴 `ci-runs-preflight` 가 주석에 속았다

파일 전체에서 `scripts/preflight.sh` 를 찾고 있었다. 그래서 `run:` 을
`echo validation-skipped` 로 바꿔도 **머리말 주석**이 조건을 만족시켜 통과했다.
게이트의 존재 이유가 「CI 가 정말 그걸 부르나」인데, 정확히 그것을 못 봤다.

- 이제 `run:` 단계만 읽는다 — 블록 스칼라(`run: |`)도 들여쓰기로 정확히 잘라낸다
- `run:` 단계가 하나도 없으면 그것도 위반이다
- 🔴 **`docs/OPEN-FINDINGS.md` 가 이걸 「닫힘」으로 적고 있었다.** 그 기록이 거짓이었다는
  사실을 지우지 않고 그 파일에 남겼다

### 🔴 주석 처리된 호출이 고아를 살려 뒀다

`// agentType: 'billing-ghost'` — 몇 달 전 꺼 놓은 줄이 에이전트를 계속 «연결됨» 으로
유지했다. 워크플로우 파일은 코드이므로 **주석을 벗기고** 매칭한다
(`//` 가 `://` 뒤면 URL 이지 주석이 아니다).

### 이 판의 증명

저장소 13규칙(**15케이스**) + 린트 9규칙(**14케이스**) + 유효변형 8.
새 케이스 둘은 위 두 결함을 그대로 재현한다.

## [2.8.1] - 2026-08-27

늦게 도착한 자문이 v2.8.0 에 남은 구멍 하나를 짚었다. 섹션만 비교하느라 **에이전트 이름이
바뀐 것을 못 봤다** — `scientist` 를 `architect` 로 바꿔도 통과했다(실측).

### 에이전트 패리티: 섹션 «만» 이 아니라 계약 전체를 본다

- **frontmatter `name`·`description` 비교.** 섹션이 전부 같아도 이름이 다르면 두 런타임이
  서로 다른 에이전트를 부르는 것이다
- **섹션 순서**를 본다. 집합만 비교하면 절이 옮겨진 것이 안 보인다
- **중복 섹션·미닫힘 섹션**을 구조 오류로 보고한다. dict 로 모으면 중복은 뒤엣것이 앞엣것을
  조용히 덮고, 미닫힘은 그 뒤 내용이 통째로 사라진다
- 정규식 대신 **줄 스캐너**로 읽는다. 본문에 XML 처럼 생긴 산문이 있어 파서는 못 쓴다
- 🔴 **런타임 이름 정규화를 좁혔다.** v2.8.0 은 `Claude`·`Codex`·`.claude`·`.codex` 를
  **전역 치환**했는데, 그러면 「`.claude/` 에 쓰지 마라」 vs 「`.codex/` 에 쓰지 마라」 같은
  **진짜 갈라짐까지 조용히 사라진다.** 이제 (섹션, 문장쌍) 으로만 선언한다 —
  자기시험이 그 반례를 고정한다
- 자기시험 10건 → **16건**

### 워크플로우는 코드다

`orphan-agents` 가 워크플로우 파일에도 산문용 폴백(백틱·`@name`)을 적용하고 있었다.
TS 에서 백틱은 **템플릿 리터럴**이라, 로그 메시지에 이름이 들어간 것만으로 고아가 사라졌다.
워크플로우에는 호출 문법만 인정한다.

### 게이트가 운영 스크래치를 레포 내용으로 읽었다

`.omc/`(에이전트 런타임 스크래치)에 자문 기록이 떨어졌고, 그 안에 인용된 코드 예시가
`dead-api` 와 `change-notice` 를 발화시켰다. **게이트는 텍스트에 대해 옳았고 그 파일이
우리 것이라는 점에서 틀렸다.** 검사 대상에서 제외하고 `.gitignore` 에 넣었다.

## [2.8.0] - 2026-08-27

두 번째 적대적 리뷰가 **결함 주입으로 직접 재현한** 6건이 있었다. 그중 하나가
가드레일 자신이었고, 그것이 나머지 「증명했다」를 전부 무효로 만들었다.

### 🔴 가드레일이 «아무 규칙도 안 돈 검사기» 를 합격시켰다

규칙 이름을 찍으면서 `exit 2` 로 죽는 가짜 검사기가 스위트 전체를 통과했다.
exit code 를 «0이냐 아니냐» 로만 봤기 때문이다. 두 검사기 모두 **1 = 지적, 2 = 못 돌았음**
으로 종료코드를 나눠 쓰는데, 판정이 그 구분을 안 봤다.

- 판정을 `verdict()` 하나로 모으고 **exit 1 + 규칙 이름 + Traceback 없음** 셋을 모두 요구
- `--self-test` 신설 — 판정기 자신을 5가지 모양으로 시험한다(과거에 두 번 속았다)
- 실증: 가짜 검사기를 넣으면 14개 케이스 **전부** 거부된다. 이전에는 전부 통과했다

### 검사기 A — 스킬 도달 (`~/.agents/skill-reach-check.sh`)

- **레지스트리를 구조적으로 읽는다.** `<skills_instructions>` 블록의 **루트 별칭 표**를
  풀어 절대경로를 확정하고 `realpath` 로 대조한다. 전에는 developer 메시지 «어딘가에»
  그 글자열이 있으면 도달로 셌다 — 설명문의 예시 경로나 **다른 루트의 동명이인**이 통과했다
- **기대 팩 집합이 자란다.** 추가는 배우고(양쪽 도달한 것만), **삭제는 절대 자동 반영하지 않는다.**
  전에는 최초 기준선에서 자라지 않아 「추가 → 통과 → 삭제」가 조용히 exit 0 이었다
- SKILL.md 가 **실제로 로드 가능한지** 본다 — 빈 파일·frontmatter 없는 파일은 도달이 아니다
- **cron 의 PATH 에 `codex` 가 없었다.** 주간 감사가 exit 2(검사 불가)로 끝나 절반이 안 돌고
  있었다. 스크립트가 자기 도구를 스스로 찾는다
- 자기시험 15건 — 산문 오염·다른 루트 동명이인·형식 변경·기대집합 생애주기 전부 고정

### 검사기 B — 에이전트 패리티 (`~/.agents/agent-parity-check.sh`)

**유사도를 버리고 섹션 단위 비교로 바꿨다.** 임계값 문제가 아니라 재는 것이 달랐다:
`Never install` → `Always install` 로 뒤집어도 98%, 실제 `tracer` 쌍에서 한 절이
통째로 빠졌는데 96% 였다. 즉 **「드리프트 0」 이라는 보고 자체가 부정확했다.**

두 파일은 포맷이 다르지만 본문은 같은 XML 이라, 런타임 껍데기만 벗기면 섹션 태그가
그대로 비교 단위가 된다. 런타임 이름 차이(`Claude Code` ↔ `Codex`)만 좁은 목록으로 정규화한다.

- 바꾸자마자 **실제 드리프트를 잡았다** — `tracer` 의 `Final_Response_Contract` 가
  Codex 쪽에 없었다. 이식해서 닫았다
- 자기시험: 한 단어 반전 · 절 삭제 · 런타임 이름 차이(정상) · 구조 판독 실패

### 검사기 C — 하네스 린트

- `agent-sections` 가 **`~~~` 펜스**를 몰랐다 — 그 안의 `##` 네 개를 진짜 섹션으로 세서,
  섹션이 하나도 없는 에이전트가 출력 예시만으로 합격했다. CommonMark 펜스 처리로 교체
- `orphan-agents` 가 전역 에이전트를 **파일명으로** 찾았다. 해석은 frontmatter `name`
  이므로, 파일명이 다른 정상 전역 에이전트 호출을 «정의 없음» 으로 오진했다
- `dead-api` 의 `team_name` 문맥 판정이 **한 줄** 단위였다. 여러 줄에 걸친
  `Agent(...)` 호출은 못 봤다 — 괄호 균형으로 호출 블록을 잡는다
- **`.claude/workflows/*.ts` 를 읽는다.** v2 의 주력 실행 모드인데 린터가 그 파일을 아예
  안 봐서, 워크플로우에서만 부르는 에이전트가 고아로 오진되고 워크플로우 안의 없는
  에이전트 호출은 안 잡혔다
- `agent-sections` 는 이제 **5개**를 요구한다. 팩토리 Phase 3 은 다섯을 지시하는데
  검사기는 넷을 세고 참조 하네스는 넷만 갖고 있었다 — 출처 셋이 답 셋이었다.
  참조 하네스에 에러 핸들링 절을 넣어 셋을 맞췄다
- `model-tiering` 에 **적어야 통과하는 면제**를 뒀다. 근거 있는 all-opus 팀과 무근거 일괄
  고정을 코드로 구분할 방법은 없다. `.claude/harness.json` 의 `uniformTierRationale` 에
  이유를 남기면 면제된다 — 내용은 사람이 읽고, «있다» 는 사실만 기계가 본다

### 배선 — 게이트를 사람의 기억에 맡기지 않는다

- **CI 가 `scripts/preflight.sh` 를 부른다.** 게이트 목록을 워크플로우에 다시 나열하면
  두 목록이 갈라지고, 그때 초록불은 «CI 가 아는 게이트만 통과했다» 가 된다
- `ci-runs-preflight` 게이트가 그 사실을 검사한다
- `scripts/install-hooks.sh` — pre-push 훅 설치. v2.7.0 이 실패한 가드레일과 함께 나간 건
  push 명령의 `&&` 사슬 밖에 있었기 때문이다
- `lint-rule-docs` 게이트 — 린터가 7규칙에서 9규칙이 되는 동안 문서 두 곳이 계속 7이라고
  말하고 있었다. **플러그인 없이 쓰는 사용자가 손으로 확인하는 목록**도 그중 하나였다
- 주간 cron 셋을 `~/.agents/weekly-audit.sh` 하나로 묶고, 실패하면 세션 시작에서 보이게 했다.
  아무도 안 읽는 로그에 쌓이는 경보는 경보가 아니다

### 그 밖

- 가드레일이 워킹트리를 복사한 뒤 `git add -A` 해서 **미추적 파일이 추적으로 바뀌었다**.
  이제 실제 저장소가 추적하는 경로만 스테이징한다 — 미커밋 수정은 계속 검증하면서,
  신선 클론 CI 가 잡는 것을 로컬에서도 잡는다
- 유효 변형 `uniform-sonnet` 이 에이전트 2개라 `model-tiering` 의 `<3` 조기반환에 걸려
  **규칙을 한 줄도 안 타고 있었다**. 3개로 늘렸다
- 요약 문구가 «12 repository checks» 였지만 실제로는 11 규칙 + 캐리어 1 이었다.
  이제 규칙 수와 케이스 수를 나눠 찍는다
- `break_skill_frontmatter` 가 `name: harness` 라는 한 가지 표기에 결합돼 있었다
- Python 3.14 `__pycache__/*.pyc` 두 개가 추적되고 있었다 — 제거 + `.gitignore`
- `CONTRIBUTING.md` 신설

현재: **저장소 13규칙(14케이스) + 린트 9규칙(12케이스) + 유효변형 8** 전부 실증.

## [2.7.2] - 2026-08-27

### Fixed

- **`--quick` 캐시가 Codex 레지스트리 변화를 못 봤다** — 지문이 팩 이름만 봐서, 레지스트리가
  사라져도 캐시가 ok 를 줬다. 이 캐시는 **SessionStart 에 배선돼 있어** 낡은 ok 가 매 세션
  반복될 수 있었다. 지문에 codex 버전·`config.toml`·`AGENTS.md`·`hooks.json` 수정시각을 넣고,
  캐시 수명 상한 24시간을 뒀다 (quick 은 255ms 유지)

### Added

- **`docs/OPEN-FINDINGS.md`** — 적대적 리뷰가 지적했으나 **아직 안 고친 것**만 모은 문서.
  전부 실측 재현된 것이고, 우선순위가 붙어 있다. 🔴 그중 B-4 는 **지금 「드리프트 0」이라는
  보고 자체가 부정확**함을 뜻한다(실제 tracer 쌍이 이미 갈라져 있는데 96%라 통과 중)

---

## [2.7.1] - 2026-08-27

🔴 **2.7.0 은 가드레일이 실패하는 상태로 배포됐다.** 원인이 검사가 아니라 «배포 절차» 였다.

### Fixed

- **`validate_repository.py` 의 finding 에 규칙 id 부착** — 2.7.0 에서 가드레일이
  「규칙 이름이 findings 에 나와야 한다」를 요구하게 강화했는데, 정작 이 검사기는 이름 없이
  `- missing required file: NOTICE` 만 냈다. 검사를 약화시키지 않고 검사기가 id 를 내도록 고침
- **`scripts/preflight.sh` 신설** — `validate && commit && push` 라고 써서 **가드레일 실패가
  체인에 없었다.** 검사를 여러 개 만들어 놓고 배포 명령에 하나만 걸면 나머지는 있으나 마나다

---

## [2.7.0] - 2026-08-27

두 번째 적대적 리뷰가 **네 검사기 전부에서 거짓통과를 재현**했다. 그중 하나는 다른 모든
증명을 무효화하는 것이었다.

### Fixed

- 🔴 **가드레일이 «아무 규칙도 실행하지 않는» 검사기를 승인했다** — 규칙 이름이 findings 에
  나왔는지를 Traceback 이 있을 때만 확인했다. 그래서 `exit 2` 만 내는 가짜 검사기로 바꿔도
  스위트 전체가 통과했다(실측). 이제 ① 깨끗한 실행이 규칙 이름을 대야 하고 ② 깨진 실행이
  그 규칙의 finding 을 내야 한다. **이 결함은 나머지 게이트의 «증명» 을 전부 무효화하고 있었다**
- **`dead-api` 가 부정어 한 단어로 뚫렸다** — `TeamCreate was removed, so call TeamCreate(x)`
  가 통과했다. 호출 «형태»(`TeamCreate(`)면 부정어와 무관하게 지시문으로 판정
- **빈 `description:` 이 9규칙 전체를 통과** — 키 존재만 보고 값을 안 봤다
- **마크다운 링크 형태의 깨진 references 미검사** — 백틱 경로만 봤다
- **중복 `name` 이 set 에서 합쳐져 사라짐** → `agent-duplicates` 규칙 신설

### Changed

- **`agent-parity-check` 의 «비쌍둥이 >60% 차단» 삭제** — 두 에이전트에 같은 공통 안전 규약을
  추가하는 정상 변경이 52%→76% 로 차단됐다. 정상 작업을 막는 검사는 꺼지고, 꺼진 검사는
  없는 것보다 나쁘다. 유사도는 정보로만 표시
- **`skill-reach-check` 에서 «검사 못 함» 이 «통과» 로 새지 않는다** — codex 부재 시
  `status:ok, exit 0` 이었다. 이제 exit 2

---

## [2.6.1] - 2026-08-27

적대적 리뷰가 찾은 린터·가드레일 결함 수정. 전부 결함 주입으로 재확인.

### Fixed

- **`orphan-agents` 가 일상 문장을 호출로 오인** — 이름이 `build`·`review` 같은 일상어면
  "we should build the binary" 한 줄에 고아가 사라졌다. 호출 문법만 인정하도록 변경
- **`model-tiering` 이 `opus` 리터럴만 봤다** — `claude-opus-4` 로 일괄 고정하면 통과했다. 계열 전체 포착
- **`agent-sections` 가 코드펜스 안의 `##` 을 셌다** — 본문에 섹션이 없어도 출력 예시만으로 합격
- **frontmatter 를 부분문자열로 판정** — `description` 값 안의 `name:` 에 속아 키가 없어도 통과
- **거짓양성 2건 제거** — 전역/빌트인 에이전트 호출을 「정의 없음」으로 잡던 것,
  `team_name` 을 Claude 문맥 밖(스포츠·조직 관리 하네스)에서 오진하던 것
- **가드레일이 크래시를 「잡았다」로 셌다** — exit code 만 봐서, 검사기가 죽어도 `ok caught`.
  그 규칙 이름이 findings 에 실제로 찍혔는지까지 확인

---

## [2.6.0] - 2026-08-27

### Added

- **`docs/pilot-protocol.md`** — 첫 하네스를 «만들기 전» 과 «만든 후» 의 기준을 하네스가
  존재하기 전에 못박아 둔다. 나중에 기준이 움직이지 못하게.
  - 착수 조건 3개(전부 충족해야): 같은 모양의 작업이 3회 이상 반복 / 도메인 전용 역할 2개 이상 /
    산출물·종료조건이 기계 검증 가능
  - 판정 기준 5개(전부 충족해야): lint 0건 / 3개 시나리오가 올바른 오케스트레이터로 라우팅 /
    **생성된 모든 에이전트가 최소 1회 실제 호출** / 사람의 라우팅 개입 없이 end-to-end /
    범용 기준선 대비 누락 감소 + 비용 증가 25% 이하
  - **하나라도 실패하면 생성물을 삭제하고 다른 서비스로 확장하지 않는다**
  - 실측 예시 포함(크롤 파이프라인 268파일·커밋 200건 분석)

---

## [2.5.0] - 2026-08-27

### Added

- **설명 이미지 5장** (`docs/images/`, 영어, README 3종 공통) — 줄글로만 있던 개념을 그림으로.
  ① 한 문장 → 에이전트 팀 ② **없는 툴은 에러를 안 낸다**(이 포크가 존재하는 이유)
  ③ 실행 모드 3종 ④ 린트 게이트 8종 ⑤ **검사기는 누가 검사하나**
  gpt-image-2 로 생성. 이미지는 언어 중립이라 세 README 가 같은 파일을 공유한다.

---

## [2.4.0] - 2026-08-27

### Added

- **`agent-naming` 규칙** — 생성 에이전트가 `analyst`·`reviewer` 같은 **범용 역할 이름을 그대로
  쓰는 것을 거부**한다. 프로젝트 스코프가 유저 스코프를 조용히 이기므로, 그런 이름 하나가
  사용자의 전역 에이전트를 이 프로젝트에서 없애 버린다. 26개 예약어.
- **`.claude/harness.json` 의 `agentNamespace`** — 선언하면 모든 에이전트가 그 접두사를 갖도록
  «강제»한다. 「접두사를 붙이세요」라는 권고가 게이트가 된다. 선언 안 하면 예약어 검사만 돈다.
- 가드레일 확장: 저장소 12 + 린트 규칙 8 + **유효 변형 4**(일본어 / `name`≠파일명 /
  전원 sonnet / 선언된 네임스페이스). 총 24개 게이트.

---

## [2.3.2] - 2026-08-27

🔴 **2.3.0/2.3.1 은 설치하면 로드에 실패한다. 이 버전으로 올려라.**

### Fixed

- **플러그인이 아예 로드되지 않았다** — `hooks/hooks.json` 과 `commands/` 는 Claude Code 가
  **관례로 자동 발견**하는데 `plugin.json` 에 또 선언해서 이중 로드가 됐다:
  `Hook load failed: Duplicate hooks file detected`. 두 필드를 제거했다
- `claude plugin validate` 는 이걸 **통과시켰다** — 매니페스트 «형태»는 검사하지만 로드
  «의미»는 검사하지 않는다. 그래서 첫 신호가 "설치했는데 안 뜬다" 였다

### Added

- **`manifest-conventions` 검사** — `plugin.json` 이 관례 경로(`./hooks/hooks.json`,
  `./commands/`, `./skills/`, `./agents/`)를 다시 선언하면 에러. 가드레일이
  «일부러 다시 선언하면 잡히는지» 를 증명한다

### 이 버그가 어떻게 나왔나

2.2.0 에서 설치 스모크를 돌려 통과시켰다. 그 버전은 `skills/` 하나뿐이었다.
2.3.0 에서 `commands/` 와 `hooks/` 를 **추가한 뒤로는 스모크를 다시 돌리지 않았다.**
저장소는 검증했고 설치본은 검증하지 않았다 — 「돌아갈 곳에서 검증하라」를 어긴 결과다.

---

## [2.3.1] - 2026-08-27

정확성 릴리스. 2.3.0 에서 낸 린터가 **자기 규칙을 스스로 어겼다.**

### Fixed

- 🔴 **린터가 우리가 시킨 대로 만든 하네스를 거부했다** — Phase 1-7 은 "생성물은 사용자 locale 을
  따른다"고 지시하는데, `agent-sections` 는 한국어·영어 제목만 찾았다. 일본어로 생성하면
  우리 린터가 우리 지시를 위반했다고 판정했다. 언어별 제목 매칭을 버리고 **구조(섹션 4개)** 를 센다
- **`agent-frontmatter` 가 `name`≠파일명을 실패시켰다** — 해석은 `name` 기준이라 둘이 달라도
  합법이다. 게다가 에러 메시지가 「닿지 못한다」고 **사실이 아닌 근거**를 댔다. 규칙 제거
- **`orphan-agents`·`user-scope-shadowing` 이 에이전트를 파일명으로 식별했다** — 같은 가정이
  다른 규칙에 복제돼 있었다. 이름 해석을 `Harness.agent_name()` 한 곳으로 통일
- **`model-tiering` 이 「전원 같은 티어」를 결함으로 봤다** — 같은 성질의 업무라면 같은 티어가
  정답일 수 있다. 실제 v1 안티패턴인 **전원 `opus` 고정**만 잡도록 좁혔다
- **스킬 탐색이 재귀가 아니었다** — 중첩 스킬을 놓쳤다. `rglob`
- README 3종·SKILL.md 에 적혀 있던 위 틀린 근거 12곳 교정

### Added

- **가드레일에 «유효 변형은 PASS 해야 한다» 단계** — 지금까지는 규칙을 깨뜨려 FAIL 하는 것만
  증명했다. 그건 규칙이 **발화한다**는 증거일 뿐 **옳다**는 증거가 아니다. 잘못된 규칙도 완벽하게
  FAIL 한다. 일본어 하네스 · `name`≠파일명 · 전원 sonnet 세 변형이 통과해야 한다.
  이 단계는 도입 즉시 `orphan-agents` 의 남은 결함을 잡아냈다

---

## [2.3.0] - 2026-08-27

이 릴리스의 한 줄: **팩토리가 «규칙을 쓰는 것»에서 «규칙이 지켜졌는지 검사하는 것»으로 넘어갔다.**
규칙을 생성하고 아무도 검사하지 않는 것이 이 도메인의 측정된 실패 모드다.

### Added

- **`scripts/harness_lint.py` — 생성된 하네스를 계약에 대고 검사하는 린터.** 대상 프로젝트의
  `.claude/agents/` 와 `.claude/skills/` 를 읽어 결정적 규칙 7종을 돌린다:
  `agent-frontmatter` · `agent-sections` · `dead-api` · `user-scope-shadowing` ·
  `skill-frontmatter` · `orphan-agents` · `model-tiering`.
  전부 **셀 수 있는** 규칙이다 — 문체·품질을 점수 매기지 않는다. 논쟁하는 검사는 꺼지고,
  꺼진 검사는 커버리지처럼 보여서 없는 것보다 나쁘다
- **`orphan-agents`** — 아무 오케스트레이터도 안 부르는 에이전트, 그리고 정의 없는 에이전트 호출.
  「에이전트 27개를 만들었는데 그중 몇 개가 실제로 도나」에 처음으로 답하는 규칙
- **`commands/` 3종** — `/oh-my-harness:harness-lint` · `harness-audit` · `harness-evolve`
- **`hooks/` — SessionStart v1 잔재 감지기.** 프로젝트가 제거된 API 를 «지시»하면 세션 시작에
  1회 경고. `.claude/agents/` 가 없으면 쳐다보지도 않고, 설명문(「제거됐다」)에는 침묵한다
- **에이전트 프론트매터 풀 스펙** — `disallowedTools` `effort` `maxTurns` `isolation: worktree`
  `memory` `skills` `background` `color` 와 «언제 쓰나» 표.
  🔴 **플러그인 배포 에이전트는 `hooks`·`mcpServers`·`permissionMode` 를 무시한다**는 함정도 명시
- **`tests/fixtures/clean-harness/`** — 린터의 기준점이 되는 «올바른» 생성 하네스 샘플
- 가드레일 확장: 저장소 검사 10종 + **harness-lint 규칙 7종**, 총 17개 게이트가 각각
  깨진 입력에서 실제로 발화함을 증명

### Changed

- **Phase 6-1 이 산문 체크리스트에서 명령 한 줄로 바뀌었다** — 「확인했다」가 아니라
  `harness_lint.py` 의 출력과 exit code 가 근거다
- `marketplace.json` 에 description 추가 (`claude plugin validate` 경고 해소)
- 저장소 스캔에서 `tests/` 전체 제외 — 일부러 깨뜨린 픽스처가 본검사를 깨지 않도록

---

## [2.2.0] - 2026-08-27

첫 파생 릴리스. 업스트림의 미머지 v2.1.0(PR #51 → 충돌해소본 #56)을 베이스로 삼고,
열린 PR 24건을 전수 판정해 채택분을 반영하고, 검사기 자체를 검증하는 CI 를 세웠다.

### Added

- **`docs/ATTRIBUTION.md`** — 업스트림 열린 PR 24건 전수 판정표(ADOPTED 10 / SUPERSEDED 2 / REJECTED 12)와 기여자 크레딧
- **`NOTICE`** — Apache-2.0 §4 귀속 고지, PR #51/#56 계보
- **`README_JA.md`** — 일본어 README 를 v2 기준으로 재작성. ⚠️ 업스트림 v2 는 **의도적으로** 이걸 뺐다("유지보수 부담 대비 효용 저조, EN/KO 2종 유지"). 그 판단이 옳았기에 되돌리는 대신 **드리프트를 탐지 가능하게** 만들었다 — `readme-parity` 검사 참조. 근거는 [docs/ATTRIBUTION.md](docs/ATTRIBUTION.md) 「One upstream decision deliberately reversed」
- **Phase 1-7 출력 locale 결정** — 생성물 본문 언어가 사용자 locale 을 따르도록. `_workspace/00_locale.md` 기록 (upstream PR #38, [@mythkiven](https://github.com/mythkiven))
- **Phase 6-7 사용자 핸드오프** — 구축 직후 트리거 예문·skill name·산출물 경로 안내 (upstream PR #6, [@gd452](https://github.com/gd452))
- **`references/self-evolution-loop.md` + Phase 7 포인터** — 증거 주도 자율 진화 루프(옵트인). 자격 게이트 A/B/C, 실패 시그니처 `(c, q, m)`, held-in/held-out 비퇴행 수용 규칙 (upstream PR #45, [@epoko77-ai](https://github.com/epoko77-ai))
- **`references/search-efficiency.md` + Phase 1-8 포인터** — Grep/Read 4-Step 탐색 프로토콜(선택) (upstream PR #12, [@namojo](https://github.com/namojo))
- **`orchestrator-template.md` Incremental QA 공통 규칙** — QA 를 마지막 단계에만 두지 않도록 전 템플릿에 검증 훅 (upstream PR #6, [@gd452](https://github.com/gd452))
- **description 중국어 트리거** — 트리거 매칭 확장 (upstream PR #23, [@hyhmrright](https://github.com/hyhmrright))
- **CI 검사 4종 신설** — `link-existence` · `dead-api` · `version-consistency` · `change-notice`
- **`size-budget` 검사** — SKILL.md 520줄 / reference 650줄 예산 (upstream PR #41, [@mythkiven](https://github.com/mythkiven))
- **`readme-parity` 검사** — 모든 `README_*.md` 가 `README.md` 와 같은 버전 뱃지·같은 최상위 섹션 수를 갖는지. 번역본의 드리프트를 «빌드 실패»로 만든다
- **`tests/guardrail/`** — 검사기 9종 각각을 일부러 깨뜨려 «실제로 FAIL 하는지» 증명하는 스위트. CI 별도 잡으로 상시 실행

### Changed

- **한국어·일본어 README 에 누락 섹션 2개 보강** — `Category` 와 `Star History`. 한국어판은 업스트림 시절부터 빠져 있었고 `readme-parity` 신설로 발견됐다
- **`oh-my-harness` 로 리브랜드** — 플러그인·마켓플레이스 이름, 저자, 저장소 URL. **스킬 이름 `harness`·`evolve` 는 유지** (트리거 문구와 v1 마이그레이션 경로 보존)
- **깨진 로컬 링크를 경고에서 에러로 승격** — 초록불 옆의 경고는 «괜찮음»으로 읽힌다. 실재하지 않는 문서 4개가 업스트림에서 몇 달간 참조된 원인
- **Phase 0 / Phase 3-0 중복 검토가 유저 스코프까지 감사** — `~/.claude/agents/` 를 함께 읽는다. 프로젝트 스코프가 유저 스코프를 덮으므로, 같은 이름을 만들면 사용자의 전역 에이전트가 경고 없이 사라진다
- **워크플로우 통합** — `harness-validation.yml` 을 `validation.yml` 로 합침

### Security

- 🔴 **이슈 템플릿이 보안 신고를 제3자에게 보내고 있었다** — 업스트림에서 그대로 상속한 `.github/ISSUE_TEMPLATE/config.yml` 이 비공개 취약점 제보를 **원저자의 회사 이메일**로 보내고, `bug_report.yml` 이 이슈를 `revfactory` 에게 자동 할당했다. 원저자는 이 파생본의 제보를 받기로 한 적이 없다. 전 경로를 이 저장소로 재배선했고 보안 채널은 GitHub Private Vulnerability Reporting 으로 교체
- **이슈 템플릿의 재현 절차가 삭제된 API 를 지시하고 있었다** — `export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 와 존재하지 않는 `harness@harness` 설치 명령. `dead-api` 가 마크다운만 스캔해서 `.yml` 을 통과시켰다 → 검사를 `.yml`/`.yaml` 로 확장하고 YAML 가드레일 픽스처 추가

### Fixed

- **LICENSE 를 Apache 공식 원문으로 복원** — 상속본이 라이선스 **본문 3곳**을 바꿔놨다(`submitted to Licensor`→`the Licensor`, `received by Licensor`→`the Licensor`, `excluding those notices`→`any notices`)+부록 삭제. 본문이 바뀐 라이선스는 Apache License 라 보기 어렵다. 원저자 저작권은 NOTICE 와 각 파일 고지에 보존
- **JSON 두 개가 스스로 변경 고지를 지니게** — `_notice` 키. Apache-2.0 §4(b) 는 "수정된 파일이 고지를 carry" 하라고 하지 JSON 예외를 두지 않는다. NOTICE 대체 주장에 기대지 않는다
- **`/harness:evolve` → `/oh-my-harness:evolve`** — 리브랜드 후 플러그인 스킬 호출은 `plugin-name:skill-name` 이라 기존 표기가 존재하지 않는 명령이 됐다
- **PR #23 채택 주장을 사실로** — 중국어 트리거를 두 매니페스트에만 넣고 `skills/harness/SKILL.md` description 을 빠뜨려, ATTRIBUTION 의 ADOPTED 가 절반만 참이었다

- **`docs/quickstart.md` 설치 명령** — 존재하지 않는 `harness@harness` 를 안내하고 있었다 (upstream PR #46, [@k002bill2](https://github.com/k002bill2))
- **충돌마커 검사의 거짓양성** — `=======` 는 마크다운 setext 제목 밑줄과 같다. 같은 파일에 여는/닫는 마커가 있을 때만 중간 마커로 판정하도록 수정
- **`dead-api` 검사의 거짓양성** — 마이그레이션 가이드·체인지로그는 제거된 API 를 «설명»해야 한다. 파일 단위 예외 + 부정어 확장으로 거짓양성 0

### Removed

- **`[Unreleased]` 섹션의 v1 잔재** — v2 가 삭제한 `references/agent-design-patterns.md` 를 가리키고 있었다

---

## [2.1.0] - 2026-07-20

### Changed

- **모델 정책 개편: "세션 모델 상속 기본" → "업무 특성 기반 티어 선택"** — 에이전트 정의 시 업무의 복잡도·작업 기간·자율성·응답 속도 4가지 기준으로 opus(설계·코드 생성·복잡한 분석·교차 검증, 계획·장기 자율 실행) / sonnet(로그 파싱·포맷 변환·단순 수집 등 일상·기계적 업무)을 에이전트별로 선택하도록 변경. 애매하면 sonnet, 무근거 일괄 지정 금지 원칙 유지
- **버전 정합성 2.1.0 동기화** — `.claude-plugin/plugin.json`·`marketplace.json`·README 뱃지(EN/KO)를 2.1.0으로 통일

### Added

- **`references/model-selection-guide.md`** — 티어별 핵심 역할·적합 업무·선택 상황, Opus vs Sonnet 구분 기준, 하네스 적용 규칙(업무 단위 판단, 계층 분리, 워크플로우 단계별 적용) 상세 가이드

## [2.0.0] - 2026-07-19

전면 재구축 (ground-up rebuild). v1의 전제였던 실험적 Agent Teams API가 현행 Claude Code에서 사라졌고, 결정적 오케스트레이션을 위한 Workflow 도구가 새로 추가된 환경 변화에 맞춰 모든 것을 다시 설계했다.

### Breaking / Fixed

- **`TeamCreate`/`TeamDelete`/`team_name` 전면 제거** — 현행 런타임에 존재하지 않는 API. 세션의 단일 암묵 팀 + `Agent(name:)` + `SendMessage` 구조로 전 템플릿 재작성. v1 오케스트레이터는 이 API를 호출하다 단일 에이전트 실행으로 조용히 퇴화하는 실질적 브로큰 상태였다
- **`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 의존 제거** — 플래그 안내 문서(`docs/experimental-dependency.md`) 및 모든 참조 삭제. v2는 플래그 없이 동작한다
- **`model: "opus"` 전 에이전트 강제 정책 폐기** — 세션 모델 상속이 기본. 오버라이드는 근거(기계적 작업의 비용 절감 / 최고 난도 검증)가 있을 때만 명시
- **README-실체 불일치 해소** — v1 README가 홍보하던 `/harness:evolve` 스킬이 실제로는 존재하지 않았다. v2에서 실제 스킬로 출시
- **"세션당 한 팀" 제약 및 팀 재구성 절차 삭제** — 제약 자체가 소멸

### Added

- **3중 실행 모드 체계** — 워크플로우 오케스트레이션(신규) / 퍼시스턴트 에이전트 협업(v1 팀 모드 대체) / 서브에이전트 위임. 모드 선택 기준을 "팀 크기"에서 **"제어 흐름의 결정성"**으로 재정의
- **워크플로우 오케스트레이션 모드** — `Workflow` 도구 기반: `pipeline()`/`parallel()`, 구조화 출력 스키마(파싱 불필요), `budget` 연동 규모 조절, `resumeFromRunId` 부분 재실행, 커스텀 `agentType`, `isolation: worktree`
- **품질 패턴 카탈로그** — 적대적 검증, 관점 분산 검증, 심판 패널, loop-until-dry, 다각 스윕, 완전성 비평가, 침묵 상한 금지
- **`skills/evolve` (`/harness:evolve`)** — 델타 수집 → 피드백 유형 분류 → 일반화 반영 → 변경 이력 갱신 → 진화 보고의 5단계 진화 스킬. 관찰 기반 진화 신호(반복 피드백, 오케스트레이터 우회 흔적) 감지 포함
- **v1 → v2 마이그레이션 경로** — Phase 0에서 v1 산출물 자동 감지, `docs/migration-v1-to-v2.md` + `references/execution-modes.md`에 변환 매핑 표
- **신규 레퍼런스** — `execution-modes.md`(3중 모드 + 마이그레이션), `workflow-recipes.md`(스크립트 스켈레톤 6종 + 함정 12종 체크리스트)
- **데이터 전달 프로토콜에 "구조화 반환" 추가** — 워크플로우 schema 기반 타입 안전 전달을 v2 권장 기본으로
- **워크플로우 기반 A/B 테스트** — 스킬 검증(with/without)을 워크플로우 스크립트로 구성하는 레시피
- **에이전트 정의 확장** — 프론트매터 `tools`(읽기 전용 리뷰어 등 도구 제한), `재호출 지침` 섹션 표준화

### Changed

- **6패턴 각각에 v2 권장 실행 모드 매핑** — 팬아웃/파이프라인은 워크플로우 1순위, 감독자는 퍼시스턴트+태스크, 계층 위임은 워크플로우 1단계 중첩 등
- **오케스트레이터 템플릿 3종 전면 재작성** — A: 워크플로우(정찰→실행→종합, run_meta/resume 포함), B: 퍼시스턴트(스폰+태스크+피드백 루프), C: 서브에이전트
- **Phase 7 재구성** — 운영/유지보수는 harness 스킬에 유지, 진화(피드백 반영)는 evolve 스킬로 분리
- 산출물 체크리스트에 v1 잔재 검증·워크플로우 함정 검증 항목 추가
- `references/agent-design-patterns.md` → `team-patterns.md`로 개편 (품질 패턴 흡수), `team-examples.md` 5종 예시를 v2 문법으로 재작성

### Removed

- **마케팅/사이트 자산 제거** — `harness_banner.png`·`harness_icon.png`·`harness_social.png`·`harness_team.png`(합계 약 9MB), `index.html`(랜딩 페이지), `privacy.html`
- **저장소 운영 부산물 제거** — 런치 캠페인용 `_workspace/` 산출물, 마케팅 에이전트 정의(`.claude/agents/launch-strategist` 등), `docs/experimental-dependency.md`
- README_JA (일본어 README) — 유지보수 부담 대비 효용 저조. EN/KO 2종 유지

## [1.2.1] - 2026-04-18

### Fixed

- **버전 정합성 동기화** — README.md / README_KO.md / README_JA.md 뱃지가 `v1.0.1`, `.claude-plugin/marketplace.json`이 `1.1.0`, `.claude-plugin/plugin.json`이 `1.2.0`으로 3중 불일치 → 모두 **v1.2.0**으로 통일 (plugin.json 기준)
- **태그드 릴리스 0건 상태 해소 준비** — v1.0.0 / v1.0.1 / v1.1.0 / v1.2.0 소급 태그 계획 작성 (`_workspace/release/audit-2026-04-18.md` §4 참조)

### Added

- **포지셔닝 선언: "harness factory"** — README 상단에 카테고리 자기 규정 문구를 도입. "에이전트 + 스킬을 도메인별로 찍어내는 하네스 팩토리"로 카테고리 선점 (단일 에이전트/프롬프트 프레임워크 대비 차별화)
- **CONTRIBUTING.md** — 기여 가이드 및 SLA 명시 (PR 1차 응답 72h, Issue triage 48h). 커뮤니티 온보딩 장벽 해소
- **docs/ 디렉토리** — 장기 문서(아키텍처, 마이그레이션, 패턴 카탈로그) 이전 공간 신설. README 비대화 방지 및 검색성 향상
- **Issue #3 응답 정책** — 커뮤니티 이슈에 대한 공식 응답 템플릿 및 트리아지 프로세스 추가

### Changed

- `.claude-plugin/marketplace.json` version: `1.1.0` → `1.2.0`
- README 뱃지 (EN/KO/JA 3종): `Version-1.0.1` → `Version-1.2.0`
- **`.claude-plugin/plugin.json` description 재작성** — `"Agent Team & Skill Architect — Meta-skill that designs..."` → `"The team-architecture factory for Claude Code — a meta-skill that turns a domain description into an agent team and the skills they use, with six pre-defined team-architecture patterns..."` (EN+KO 병기, L3 Meta-Factory 포지셔닝 반영)
- **`.claude-plugin/plugin.json` keywords 확장** — 5개 → 17개 (`harness-factory`, `team-architecture-factory`, `claude-code-plugin`, `agent-scaffolding`, `multi-agent`, 6패턴 키워드 6종 추가)

## [1.2.0] - 2026-04-08

### Changed

- **CLAUDE.md 등록 정책 간소화 (중복 제거)** — Phase 5-4 "컨텍스트 등록"을 "포인터 등록"으로 전환. 에이전트 목록·스킬 목록·디렉토리 구조·실행 규칙 상세를 CLAUDE.md에서 제거하고 **트리거 규칙 + 변경 이력**만 남김. 에이전트/스킬 목록은 `.claude/agents/`, `.claude/skills/` 및 오케스트레이터 스킬에서 단일 출처로 관리
- **Phase 3/4 임시 동기화 단계 삭제** — CLAUDE.md 동기화 부담을 줄이기 위해 Phase 3/4의 임시 동기화 지시 제거. 최종 포인터 등록은 Phase 5-4에서 1회만 수행
- **핵심 원칙 3번 재정의** — "CLAUDE.md에 하네스 컨텍스트를 등록한다" → "CLAUDE.md에 하네스 포인터를 등록한다"
- **CLAUDE.md vs 오케스트레이터 역할 분담표 삭제** — 포인터 정책으로 단순화되어 표 자체가 불필요해짐

### Added

- **Phase 2-1: 하이브리드 실행 모드** — 에이전트 팀 / 서브 에이전트에 더해 Phase별로 모드를 섞는 하이브리드 패턴 추가. 자주 쓰이는 조합(병렬 수집→합의 통합, 팀 생성→검증, Phase 간 팀 재구성) 명시
- **Phase 2-1 실행 모드 비교표** — 팀/서브/하이브리드 3종 특성 및 의사결정 순서 3단계 제공
- **Phase 5-0 하이브리드 오케스트레이터 패턴** — 하이브리드 구성 시 각 Phase 상단에 실행 모드를 명시하는 규칙
- **Phase 5-1 반환값 기반 데이터 전달** — 서브 에이전트 모드 전용 데이터 전달 전략 추가 (기존 메시지/태스크/파일 + 반환값)
- **Phase 5-1 권장 조합 (서브/하이브리드)** — 팀 모드 외 서브 모드와 하이브리드에서의 데이터 전달 권장 조합 명시

## [1.1.0] - 2026-04-05

### Added

- **Phase 0: 현황 감사** — 트리거 시 기존 하네스 상태를 먼저 확인하고 신규 구축/기존 확장/운영·유지보수 3분기로 라우팅
- **기존 확장 Phase 선택 매트릭스** — 에이전트 추가/스킬 추가/아키텍처 변경별 필요 Phase를 명시한 결정표
- **Phase 3/4 CLAUDE.md 임시 동기화** — 에이전트·스킬 생성 직후 CLAUDE.md에 즉시 반영 (세션 중단 내성)
- **Phase 5-4: CLAUDE.md 하네스 컨텍스트 등록** — 에이전트 팀 구조·스킬 목록·실행 규칙·디렉토리 구조·변경 이력을 기록. CLAUDE.md vs 오케스트레이터 역할 분담표 포함
- **Phase 5-5: 후속 작업 지원** — 오케스트레이터 description에 후속 키워드 필수 포함, Phase 0 컨텍스트 확인 단계로 초기/부분재실행/새실행 자동 판별
- **Phase 5 오케스트레이터 수정 경로** — 기존 확장 시 오케스트레이터를 새로 만들지 않고 수정하는 가이드
- **Phase 7: 하네스 진화 메커니즘** — 실행 후 피드백 수집 → 피드백 유형별 수정 대상 매핑 → 변경 이력 기록 → 자동 진화 트리거
- **Phase 7-5: 운영/유지보수 워크플로우** — 현황 감사→점진적 수정→CLAUDE.md 동기화→변경 검증 4단계
- **description에 운영/유지보수 트리거** — '하네스 점검', '하네스 감사', '하네스 현황', '에이전트/스킬 동기화' 키워드
- **산출물 체크리스트 강화** — CLAUDE.md 동기화 완료, 변경 이력 기록, Phase 0 컨텍스트 확인 항목 추가
- 오케스트레이터 템플릿에 Phase 0 (컨텍스트 확인) 추가 — 에이전트 팀/서브 에이전트 모드 모두 적용
- 오케스트레이터 description 템플릿에 후속 작업 키워드 패턴 포함

### Changed

- 핵심 원칙 2개 → 4개로 확장 (CLAUDE.md 등록, 진화 시스템 추가)
- **"진화 로그" → "변경 이력" 통일** — 이름과 스키마(4컬럼: 날짜/변경내용/대상/사유)를 전 섹션에서 일원화
- **Phase 1 Step 3** — Phase 0 감사 결과를 기반으로 충돌 분석하도록 변경 (중복 제거)
- **5-4 CLAUDE.md 템플릿 코드 블록** — 중첩 렌더링 깨짐 수정 (3백틱→4백틱)
- **역할 분담표 확장** — 스킬 목록, 디렉토리 구조, 변경 이력 행 추가
- **오케스트레이터 템플릿** — Phase 0 컨텍스트 확인 단계, 후속 작업 키워드 가이드 추가

## [1.0.1] - 2026-03-28

### Changed

- SKILL.md ↔ references 간 중복 내용 제거 (330줄 → 285줄)
  - Phase 2-1: 실행 모드 비교표/불릿 → 핵심 원칙 + agent-design-patterns.md 포인터
  - Phase 2-3: 에이전트 분리 기준 불릿 → 4축 요약 + agent-design-patterns.md 포인터
  - Phase 3: 에이전트 정의 템플릿 코드블록 → 필수 섹션 나열 + references 포인터
  - Phase 5-2: 에러 핸들링 5행 테이블 → 핵심 원칙 + orchestrator-template.md 포인터

## [1.0.0] - 2026-03-27

### Added

- 6 Phase 워크플로우 기반 하네스 구성 메타 스킬
- 6가지 에이전트 아키텍처 패턴 (파이프라인, 팬아웃/팬인, 전문가 풀, 생성-검증, 감독자, 계층적 위임)
- 에이전트 팀 / 서브 에이전트 실행 모드 지원
- Progressive Disclosure 기반 스킬 생성 가이드
- 오케스트레이터 템플릿 (에이전트 팀 모드 + 서브 에이전트 모드)
- QA 에이전트 통합 가이드 (실제 프로젝트 7개 버그 사례 기반)
- 스킬 테스트/평가 방법론 (With-skill vs Without-skill 비교)
- 실전 팀 구성 예시 5종 (리서치, 소설, 웹툰, 코드리뷰, 마이그레이션)
